---
name: telegraph-pad-pool-pattern
description: 텔레그래프 장판 풀(TelegraphPool/TelegraphPad)·근접 격투 드라이버(Dimax/Crassorrid) 리뷰의 패드누수·_activePad스테일·ForceFull경합 추적법 + ★BruteSlamCoordinator(static 방위 등록부) stale 누수 3중차단·조기리턴 누수창·동프레임 race 분석
metadata:
  type: project
---

**2026-06-14 Crassorrid 1:1 교전 리듬 재설계(스탠드오프 정렬·정면콘 커밋·★음수 speed Run_RM 역재생 백오프·딜레이 쿨다운·FacePlayerTurn) 리뷰 — Critical 0, High 0, Medium 2, Low 3. ★최우선우려 "음수 speed가 슬램으로 누수=역재생/정지 치명"=완전 반증:**
- **★★음수 speed 백오프 누수 = 불가능(4중 방어, 최우선우려 반증).** 음수 `modelAnimator.speed` 설정 지점 단 1곳(:456 `d<nearEdge` 백오프 분기), 그 분기는 Approach 상태(`s==SApproach`) AND `!_smashFired` AND `coordinatorAllows` 안의 `if/else if` 체인 *마지막에서 두 번째*. 누수 막는 4겹: ①Update 최상단 매프레임 무조건 `modelAnimator.speed=1f`(:364) — 모든 이탈/전이 경로 커버 ②커밋 분기(:441 inBand&&facing&&cooledDown)는 음수 분기와 *상호배타 if/else if*(:441→:452→:458→:463), 같은 프레임 둘 다 실행 불가 ③커밋 분기가 SetTrigger(PSmash) *직전* `modelAnimator.speed=1f` 명시 재설정(:446) ④Approach→Windup은 CUT(hasExitTime=0, dur0 — controller :235-238)지만 *다음 프레임* 전이 → 그 프레임 Update 최상단이 다시 1f 리셋. 슬램 상태(Windup/Strike/Recovery)는 Approach 분기에 안 들어가 음수 못 받음. **음수 박제 경로 0**(:364가 every-frame 무조건이라 stale 음수 불가능). state.speed는 전역 speed에 *곱*해지므로 음수×양수state=역재생이나, 음수 도달 자체가 Approach+근접+미커밋에서만 → 슬램 진입 즉시 1f.
- **★음수 백오프 무한후퇴/진동 = 수렴(반증).** d<nearEdge면 음수→Run_RM 역재생 후퇴. 후퇴하면 d 증가→nearEdge 도달 시 :463 else(speed=0 정지)로 → 진동 아닌 *수렴*(밴드 진입=정지). farEdge 넘어가면 전진. 3분기(가까움 후퇴/밴드 정지/멀리 전진)가 standoff로 수렴. 단 ★M-1: nearEdge/farEdge 경계 정확히 d일 때 부동소수 떨림 — d==nearEdge면 백오프 아님(`d<nearEdge` 엄격), inBand는 `>=nearEdge`라 경계 정확히 닿으면 정지/커밋 쪽. 진동 윈도는 1프레임 이동량(approachSpeed*dt*backoff≈5*0.016*0.8=0.064m)이라 좁고 게임감 영역. 사고 아님.
- **★백오프 중 슬램 못함 = 정합(반증).** d<nearEdge 음수 분기는 `else if`라 커밋 분기(:441)와 배타 → 백오프 프레임엔 SetTrigger(PSmash) 절대 안 섬. inBand 판정(`d>=nearEdge`)이 백오프 영역(d<nearEdge) 배제. 백오프→d증가→밴드 진입→정면&쿨다운이면 커밋, 매끄러움. _smashFired는 커밋서만 true→커밋 후 Approach 잔여 프레임 no-op(:414).
- **★쿨다운 `_slamCooldownUntil` Time.time = 히트스탑/정지서 의도대로 멈춤(반증, 단 인지).** Time.time은 timeScale 스케일됨 → 히트스탑(0.05) 중엔 거의 안 흐름·일시정지(timeScale 0) 중엔 완전 정지 → 쿨다운 카운트다운도 같이 멈춤. **이게 올바름**: 게임 정지 중 쿨다운 까먹기 방지(unscaled면 정지 중에도 쿨다운 소진=부정확). 회수(Recovery)에서 `_slamCooldownUntil=Time.time+restBeforeApproach`(:500) 무장도 Time.time 일관. 첫 슬램=초기값 0(ResetCombatState :224)이라 `Time.time>=0` 즉시 true=지연 0(정상 오프너). 풀링 stale=ResetCombatState가 0 리셋(:224)=대칭. unscaled 불요.
- **★FacePlayerTurn/콘/방위 0벡터 = 전부 가드(반증).** FacePlayerTurn(:345 PlanarToTarget→:329 sqrMag<0.0001 model.forward 폴백)·PlayerInFrontCone(:336 fwd.sqrMag<0.0001 return false=커밋 안 함)·AzimuthOf(BruteSlamCoordinator:39 dx²+dz²<1e-6→0). 플레이어가 브루트와 정확히 같은 XZ면 콘 false→커밋 안 됨(안전, 백오프로 빠짐). NaN 표면 0.
- **★orphan(_restTimer/FaceSteer) 정리 = 깨끗(grep).** 드라이버·빌더·스포너 전수 _restTimer/FaceSteer 라이브 0(주석만). Idle 교전 분기가 쿨다운 모델로 바뀐 것 정상: Idle `!_engaged`=오프너 1회, `_engaged`=즉시 SetApproaching(true)→Approach서 백오프/square-up이 딜레이 채움(멀뚱 Idle 아님). 컴파일 경고 0.
- **★조율자·수명가드 무손상(반증, 이전 H-1 픽스 생존).** :361·:365 조기리턴 *둘 다* UnregisterSlam 포함(이전 H-1 "target null 조기리턴이 Unregister 건너뜀"=이번 코드에 픽스 반영됨 — :361 `modelAnimator==null` + :365 `target==null||model==null` 양쪽 ReleaseToken+CancelTelegraph+UnregisterSlam). :378 엣지가드(슬램상태 아닌데 등록=해제)·:499 SRecovery 정상해제 3중 보존. 즉시 RegisterSlam(:449-450 커밋프레임)=동프레임 race 차단 유지. controller AnyState 0·모든 슬램전이 무조건 ExitTime(:240-246 ChainCut 0.99/0.99 + tRecovIdle 0.95)=Recovery 경유 보장. FacePlayerTurn은 Windup(:476)까지만(cocking 조준 허용), Strike(:489)/Recovery(:494) 회전0 엄수=헌법 회전경계 무침범.
- **M-2(잠복, 이전 승계): staggerMin=0(Range 하한 0) 시 박자가드 무력→같은각+같은프레임 동시슬램 이론상 가능(직렬 Update라 실질 희박). staggerMin>0(기본 0.2) 유지가 안전선. 게임감=유저 ▶.
- **L: setup 주석 stale(:38-40 WindupSpeed 0.5/StrikeSpeed 1.25/RecoverySpeed 1.4)이나 *값은* `CrassorridBrawler.X` const 참조라 실제 빌드값 정확(0.6/3.4/0.65). 컨트롤러 YAML SmashRecovery m_Speed=0.65 확인=SetupData 재실행 반영됨. 주석만 옛 숫자(혼동 유발, 비기능). SSOT 자체는 무결.**
- **L: backoffAggression `[Range(0f,1f)]`·standoffBand `[Min(0.1f)]`·frontConeDeg `[Range(10,90)]` 인스펙터 클램프 有 → 음수/0 백오프배율·band 0除 방어. 단 slamStandoff(:58) 무가드 — slamStandoff<standoffBand면 nearEdge 음수(=백오프 영역 소멸, 항상 inBand 하한 통과). 런타임 NRE 아님(게임감), 기본 3.5>1.0이라 무발현.
- **★★H-1 target/model==null 조기리턴이 UnregisterSlam 건너뜀(유일 실수정 항목).** Update 실행순서 = `:329 if(target==null||model==null){ReleaseToken();CancelTelegraph();return;}` → (도달못함) `:342 슬램 엣지가드(if _slamRegistered && s∉{Windup,Strike,Recovery} → UnregisterSlam)`. 조기리턴이 엣지가드 *위*라, 슬램 등록(Windup/Strike)된 브루트의 target이 null로 빠지면 매프레임 :329 반환→엣지가드·SRecovery 분기 영영 안 돎. CancelTelegraph는 부르나 **UnregisterSlam은 안 부름** → 그 방위가 `ActiveSlamAzimuths`에 박제, 같은각(±angleSpread 90°) 산 브루트 차단. **소프트락 아님 이유: OnDisable이 회수**(브루트 비활성/파괴 시 풀림). 영구화=「target null인데 브루트는 계속 활성」(리타게팅 한 프레임+ 유지). 랩=target 플레이어캡슐 불사라 미발현, **production 리타게팅/플레이어사망 처리 붙으면 발현.** 픽스=:325·:329 조기리턴에 `UnregisterSlam();` 추가(`_slamRegistered` 가드라 중복/비등록 호출 무해). dict 키 메모리 미세누수도 동반(L-2) → 같이 해소.
- **★컨트롤러 AnyState 전이 0 + 모든 슬램전이 무조건 ExitTime(CrassorridBrawler.controller 실독).** Idle→Roar(attack)·Idle/Roar→Approach(isApproaching)·Approach→SmashWindup(smash) 외, **Windup→Strike→Recovery→Idle은 전부 조건없는 hasExitTime(0.99/0.99/0.95).** 슬램 진입 후 트리거로 중단 불가=「Strike 건너뛰고 Idle로 튀는 비정상」 경로가 controller상 *실재 안 함* → 슬램은 항상 Recovery 경유=SRecovery UnregisterSlam 발화 보장. :342 엣지가드는 controller가 후에 바뀌어도 자가치유하는 방어층(모범, 현재는 휴면).
- **★즉시 RegisterSlam = 동프레임 각 race 닫음(직렬 Update 전제).** Approach 커밋(:392 CanSlamNow→:400-401 _slamRegistered=true+RegisterSlam) Windup 1프레임 지연 안 함. Unity Update는 한 프레임 *직렬*→먼저 도는 브루트가 등록, 나중 브루트가 봄. 각 충돌 실질 차단. **M-1 잔여: staggerMin=0(노브 하한 Range(0f,...))이면 박자가드(:58 `!ContainsKey(self) && Time.time-_lastCommitTime<staggerMin`) 무력화** → 같은각+같은프레임+순서역전 시 이론상 둘 다 통과(회피불가 동시 내려찍기 1회). staggerMin>0 유지가 안전선. 게임감=유저 ▶.
- **M-2 OpenestAzimuth 15° 양자화 plank jitter.** 막힌 브루트 매프레임 RepositionToFlank→OpenestAzimuth(:104 cand=i*15f 24샘플 이산). 피어 과도기 변동 시 최선후보 15° 점프 가능, `slotAngleDeg=Lerp(...,repositionAggression=1f)`면 즉시 스냅=덜덜. 완화: 타이브레이크 0.5° 히스테리시스(:112-114 현재방위 근접선호)+미끄러질수록 desiredDelta→0 수렴(보고대로 안정). 진동은 과도기만. 사고 아님(게임감), repositionAggression<1로 완화.
- **M-3 피어4기 균등각 시 OpenestAzimuth가 myAzimuth(제자리) 반환=「다 막힘」.** 0/90/180/270 점유면 모든 후보 minClear 평탄(45°)→동률 타이브레이크가 제자리 선택→막힌 브루트 안 움직임(원래 없애려던 기웃). degenerate(NaN/0벡터) 아님(:39 0벡터가드). angleSpread 90°면 4분면 4기까지가 설계 한계=5번째는 갈 곳 없음(정상). 사고 아니라 설계 경계.
- **★L-1 수게이트 제거 잔재 inert 확정(안전).** `_holdsToken`(:159) 기본 false, 어디서도 true 안 됨→`ReleaseToken`(:221 `if(_holdsToken&&...)`) 영구 no-op. `tokenPool.TryAcquire()` CrassorridBrawler서 호출 0건(grep). AttackTokenPool.Release `if(_inUse>0)` 가드라 설사 불려도 공유풀 음수화 0=**Caniathrox 등 공유사용자 안 깨짐.** tokenPool 주입 보존(:124). 토큰누수0·공유풀손상0. `_holdsToken`/`ReleaseToken` 전체 dead=차후정리 후보(원래있던 코드라 삭제강요 안 함).
- **★L-2 ActiveSlamAzimuths 키=`object`, null/degenerate 키 0.** RegisterSlam 항상 `this`→null키 불가. [RuntimeInitializeOnLoadMethod(SubsystemRegistration)] 리셋(BruteSlamCoordinator:27)으로 플레이마다 정적상태 청소=씬리로드 이전런 잔존0(첫 Awake보다 앞섬). dict는 Remove만+Clear는 리셋에만→H-1 누수 시 죽은키 잔존=메모리 미세누수(키당 수십바이트, 런끝까지). H-1 고치면 동반해결.
- **★L-3 AzimuthOf degenerate 안전.** :39 dx²+dz²<1e-6→0(동일위치 NaN 회피). AngleDelta=Mathf.DeltaAngle wrap 안전. 경계입력 견고.

**2026-06-14 Crassorrid 슬램 *임팩트 주스*(VFX+카메라쉐이크+히트스탑) 추가 리뷰 — Critical 0, ★High 2(쉐이크 무동작), Medium 2, Low 4. ★최우선우려 "히트스탑 영구정지"=완전 반증(위임설계 정확):**
- **★★H-1 카메라 쉐이크 = 런타임 부착 경로에서 *영구 무동작*(silent no-op) — `MMWiggle.PositionActive=false` 기본값.** `SmashFeel.EnsureListeners`가 `AddComponent<MMCameraShaker>()`(MMCameraShaker.cs:78 `[RequireComponent(MMWiggle)]`)→빈 MMWiggle 동반 부착. **MMWiggle.PositionActive 기본 false**(MMWiggle.cs:202)→처리루프 `UpdateValue` 첫 가드 `if(!valueActive)return false`(:382)→`ProcessUpdate`가 localPosition 영영 안 건드림. `ShakeCamera`/`WigglePosition` 어디도 `PositionActive=true` 안 켬(Assets/_Project 전수 세터 0건). 씬에 사전구성 MMCameraShaker도 없음(Greybox_CrassorridLab.unity 부재 확인). → **쾅 순간 화면 안 흔들림.** artist 자체리뷰 "useUnscaledTime로 쉐이크 정상"이 실제론 *흔들림이 트랜스폼에 도달 못 함*. 강도튜닝(게임감) 아니라 기능死=버그.
- **★★H-2 쉐이크 살려도 LabSimpleCamera가 매프레임 덮어씀(설계상충).** MMWiggle은 `Update`(기본 UpdateMode)서 `transform.localPosition` 기록(:354-357), LabSimpleCamera는 `LateUpdate`서 `transform.position` 통째 덮음(LabSimpleCamera.cs:62, 매프레임 무조건). JudgeCam=루트(localPosition==position)→흔들림이 LateUpdate에서 지워짐+MMWiggle initialValue 기준점도 추종이 매프레임 이동시켜 무의미. **H-1만 고치면 H-2가 지움=한 묶음.** 추종+쉐이크 같은 트랜스폼·LateUpdate 마지막 위치확정 구조에선 쉐이크 구조적으로 못 삼 → 쉐이크 전용 자식 트랜스폼 분리 필요. ★교훈: **런타임 AddComponent하는 Feel 쉐이커는 (a)동반 MMWiggle의 `PositionActive=true`를 명시 세팅해야 작동 + (b)추종 카메라 리그면 쉐이크 전용 child 분리 필수.** 둘 다 빠지면 "쉐이크 코드는 완벽한데 화면은 안 흔들림"의 무음 함정.
- **★★히트스탑 영구정지 = 완전 안전(반증, 최우선우려).** SmashFeel이 `Time.timeScale` 직접 안 만지고 `HitStop.Do()`에만 위임(SmashFeel.cs:49)=단일소유. HitStop 중첩안전: ①연타=`_resumeAt=Mathf.Max(...)` 연장만(HitStop.cs:34), `if(!_active)`로 0.05 최초1회(:32)→한쪽 조기복원으로 타 히트스탑 깨는 경로 없음 ②히트스탑 중 브루트死=HitStop은 별도GO(Do가 자체생성)라 생존·정상복원 ③씬전환=OnDestroy `if(_active)timeScale=1`(:49). 0.05(>0)라 unscaled Update 계속 돎=복원보장, AmbientBed/VocalDirector `timeScale>0f` 가드도 active유지. **영구정지 불가능.**
- **M-1 SmashShock 셰이더 Always Included 미등록=빌드 충격파 소실(에디터무관).** GraphicsSettings.asset에 `SmashShock` 0건 확인. `Shader.Find("ZombieCrush/SmashShock")`(SmashImpactPool.cs:46) 빌드 스트립→null. ★가드 견고(셰이더null→충격파만 생략·먼지/그을림 유지 :47-51, SmashImpactFX.cs:62 `new Material(null)` 분홍회피)=분홍/NRE 없음(부분동작). 단 빌드서 충격파링(임팩트 핵심 "쾅") 통째 소실+에러로그만. 셰이더 위치 비표준(`Assets/_Project/Setting/`, 타 셰이더는 `Shaders/`). 픽스=Always Included 등록(InkBlob/UIAdditive 전례).
- **M-2 `_telegraphOrigin` 폴백 stale(telegraphPool 미주입 시).** 정상경로 안전(`_hasTelegraphOrigin` 약속지점 1회소비 :461). 단 telegraphPool==null이면 SpawnSmashTelegraph 조기리턴(:393)→`_hasTelegraphOrigin` 미세트→SmashHit 폴백이 *현재* model.forward 사용(:456). Strike는 회전0이나 루트모션 전진(3.5m)했으므로 폴백원점=전진위치 기준=약속과 어긋남. 랩 항상 주입(Spawner:95-96)이라 미발화. 통합시 "impactPool 주입=telegraphPool 동반" 못박기.
- **L: 이중 time-owner 아키텍처(랩무관, 통합점검).** HitStop 외 직접 timeScale 소유자: CarHitFeedback(0.4+fixedDeltaTime), RunManager/GameOverUI/UpgradeMenuUI/LevelUpChoiceUI/ChestRewardUI(0↔1). SmashFeel은 위임으로 회피(✅). 통합시 히트스탑(0.05) vs 일시정지패널(0) 복원경합 점검(HitStop.cs:31 주석 인지).
- **L: HitStop fixedDeltaTime 미조정(0.05중 물리스텝 1/20).** 50ms라 FixedUpdate 1~2회 스킵, Crassorrid 루트모션이라 무관. 게임감.
- **L: SmashImpactFX `_gen`/`Gen` 미사용 죽은코드.** 드라이버가 `fx.Play()` 후 참조 버림(_activePad와 달리 보관 안 함)=ProjectilePool/AcidGlob 동형. RecycleOldest 남의fx 회수해도 stale 오발 표면 없음→gen가드 불요. 비동기 대비 아니면 미사용.
- **L: 임팩트풀 누수/이중반납=0(추적).** Acquire/RecycleOldest/Return TelegraphPool 동형, `!_idle.Contains` 가드(:110)+ForceDeactivate후 Play 재초기화+Update `_active=false`후 조기리턴 재Return차단. maxTokens=1라 동시활성≤1≪poolSize8=RecycleOldest 압력0.

**2026-06-14 Crassorrid(LV4 브루트, ★TelegraphPad/Pool의 *실제 첫 소비자*) 스매시+장판 슬라이스 리뷰 — Critical 0, High 0, Medium 2(잠복·게임감), Low 3. 공유시스템 회귀 0·패드누수 0:**
- **★프롬프트 최우선 "패드 누수" = 완전 방어(반증).** 드라이버 OnDisable→CancelTelegraph→`_activePad.CancelImmediate(_activeGen)`(gen가드)→Deactivate+Return. SmashHit(임팩트)에서 ForceFull 후 `_activePad=null`(이중반납 차단). Windup 진입 전 죽으면 패드 미스폰(스폰은 Windup 진입 1회뿐). Windup~임팩트 사이 죽으면 CancelImmediate가 즉시 회수=시체위 거짓장판 0. Dimax 슬라이스의 H-1(즉시취소 API 없음)이 CancelImmediate(gen)/ForceFull(gen)/Gen 추가로 *이미 해소*된 위에 Crassorrid가 정확히 올라탐.
- **★InitPoolSize 빌드무력화 수정 = 진짜 고침(검증).** 기존 캡처들이 쓰던 `SerializedObject.FindProperty("poolSize")` 주입은 *에디터 전용*(빌드에 UnityEditor 없음)→빌드서 poolSize=기본12로 무시될 함정. 신규 `public InitPoolSize(int)`는 순수 런타임 메서드(#if 없음)+`_built` 가드(prewarm 후 호출=무시 경고)+`Mathf.Max(1,size)`(0/음수 방어). 스포너가 inactive GO에 AddComponent→InitPoolSize→SetActive(true)로 Awake/Build 전 주입=prewarm에 반영. 순서 정확.
- **★공유풀 Acquire/RecycleOldest/Return = ProjectilePool과 구조동형(검증).** 동시 다수 Crassorrid 점유도 멤버십 1곳 유지(Begin 재초기화로 RecycleOldest 이중 live 추가 무해, 기존 메모 §3 동일). 토큰풀 maxTokens=1(랩 기본)이라 동시 활성 패드 ≤1, poolSize 8 한참 밑→RecycleOldest 압력 0→gen-stale ForceFull 휴면.
- **★토큰 회계 = 누수 0(추적).** 게이트 `_holdsToken || (tokenPool!=null && TryAcquire())` — _holdsToken 참이면 단락(이중획득 0). Release는 Idle 분기 매프레임(:263)+OnDisable+Update null가드. 스매시 사이클(Approach획득→Windup→Strike→Recovery→Idle해제)에서 Idle 복귀까지 토큰 홀드=의도(한 마리씩 슬램). rest 0.5s도 Idle이라 그동안 Release 호출됨. AttackTokenPool.Release `_inUse>0` 언더플로우 가드.
- **★H-2 윈드업 배율 누수 = 방어(검증).** Approach 배율(`approachSpeed/RunNativeSpeed`=0.522)을 `if(!_smashFired)`로 게이팅+smash 발동 시 즉시 `modelAnimator.speed=1f`. Update 맨 위 매프레임 `speed=1f` 리셋이 이탈 경로 전체 커버. Windup state.speed(0.5)에 배율 안 곱해짐.
- **★_windupSpawned 상태기반 엣지가드 = Strike 스킵 경로까지 자가치유(검증).** `if(s!=SWindup) _windupSpawned=false`(:258) — Strike 분기에서만 리셋하던 (구) 버전이면 Strike 건너뛴 비정상 복귀에서 stale true 잔존→다음 Windup 무음 미스폰. 상태기반이라 모든 비-Windup 프레임이 재무장.
- **★fillDuration 산술 = 경계 안전.** SmashWindupToImpactSeconds=windup(15/30/0.5=1.0s)+strike(5/30/1.25=0.133s)=1.133s. const(WindupSpeed/StrikeSpeed/SrcFps/WindupFrame/ImpactFrame) 단조양수라 0除 없음. Begin이 `Mathf.Max(0.01f, fillDuration)`로 최종 방어. SmashContactNorm=(20-15)/(30-15)=0.333 [0,1] 안전.
- **★SSOT const = desync 불가(grep).** WindupSpeed/StrikeSpeed/RecoverySpeed/SrcFps/WindupFrame/ImpactFrame 전부 드라이버 public const 단일정의→빌더가 `CrassorridBrawler.X` 크로스참조. fillDuration(드라이버 런타임)↔이벤트 norm(빌더 임포트)이 같은 WindupFrame/ImpactFrame 공유=정합.
- **★SmashHit SendMessage 도달 = Awake 어서션 방어.** AnimationEvent는 Animator와 같은 GO의 컴포넌트에만 SendMessage 도달. 스포너가 드라이버를 프리팹 루트(=Animator GO, Crassorrid는 Animator 루트)에 AddComponent+Awake `if(modelAnimator.gameObject!=gameObject) LogError`. DontRequireReceiver라 미도달도 무에러(단 ForceFull 동기 실패=장판 채움이 클립과 미세 어긋남, 시각만).
- **M-1(잠복): Idle/Roar/Approach/Recovery 루트모션 미측정 → 비-0이면 평시/회수 중 드리프트(Dimax 6차 M-1과 동근).** 드라이버는 OnAnimatorMove 미구현=Unity 자동 루트모션 적용(게인 없음, 클립 그대로). SmashAttack_RM만 실측(전진3.514·측면0·상승0). Idle/Roar/Run_RM은 root 본 baked 변위가 자동 통과 — Idle에 translation 구워졌으면 제자리 대기 중 슬금슬금 밀림(Roar 5×압축이라 짧은시간 확). Crassorrid는 게인 없어 ×1.3 증폭은 없으나(Dimax와 차이) 자동적용 자체는 살아있음. 디스크 측정 불가(런타임 SampleAnimation 필요). 게임감 영역이나 *제자리 위치버그* 가능성이라 기록.
- **M-2(잠복, 게임감): 스매시 3분할 CUT 경계 2개(frame15/30) 루트XZ 연속성 코드 미증명.** `keepOriginalPositionXZ=false`라 firstFrame≠0 sub-clip은 루트XZ가 클립 첫프레임 상대 리베이크. 같은 take라 대개 매끄럽지만 경계 스냅을 빌더검증(:135 길이·이벤트개수만)이 증명 안 함. Dimax 4차 M-1 승계. 픽스=육안1회 or 빌더 rootT델타 로그. 유저 ▶.
- **L: SmashHit 데미지 미연결(빈 훅, 의도).** :391 광역히트 주석만, 통합 시 안 채우면 장판은 차오르는데 데미지 0(무음). 통합 체크리스트.
- **L: telegraphForwardOffset/Radius 음수가드 부분만.** SpawnCircle radius=`Mathf.Max(0.05f)` 방어 O. forwardOffset은 무가드(음수면 장판이 뒤에 깔림=게임감 파탄, 런타임NRE 아님). Min(0f) 없음(SerializeField 노브). 유저가 음수 안 넣으면 무관=Low.
- **L: telegraphPool==null이면 장판 생략(무음, 의도된 폴백).** SpawnSmashTelegraph 첫줄 `if(telegraphPool==null) return` — 토큰풀 null=공격금지(반전 footgun)와 달리 텔레그래프는 null=장판없이 스매시 진행(모션만). 통합 시 풀 주입 누락하면 장판 무음 소실(에러 아님). 랩 항상 주입.

2026-06-13 Dimaxillosaurus 근접 격투 수직 슬라이스(TelegraphPad/TelegraphPool/DimaxillosaurusBrawler + 랩) QA 리뷰에서 확인. 토큰 누수는 깨끗이 승계(Venodonte 패턴 동일), 신규 위험은 전부 **장판 패드 수명·드라이버↔패드 참조 경합**에 몰림.

**Why:** 나머지 장판 종(스매시 원·도약 착지·가스 링)이 이 풀을 재사용한다(SpawnCircle/SpawnRing 이미 존재). 토큰 풀과 달리 패드는 *드라이버가 참조를 들고 나중에 다시 만진다*(_activePad.ForceFull) — 토큰엔 없던 새 결합 클래스라 따로 갈라둔다.

**How to apply (장판 패드 풀/근접 텔레그래프 드라이버 리뷰 시):**

1. **★패드 누수 = 토큰 누수의 텔레그래프版 (최우선, 이번 리뷰 H-1).** 드라이버 OnDisable이 ReleaseToken은 부르지만 *활성 패드 정리는 안 함*. 콤보 중 몬스터 사망/비활성 시: 토큰은 반납되나 스폰된 패드는 자기 Update(`Begin`이 _active=true)로 계속 채움→홀드→자가 Return하므로 *풀로는 돌아온다*(영구 누수 아님). **단 패드는 드라이버가 죽어도 fillDuration+hold만큼 화면에 남아 끝까지 채워진 뒤 "닿는" 거짓 신호를 낸다**(시전자는 이미 죽었는데 장판은 가득 참). 누수보다 *공정성 거짓말*이 문제. 점검: 드라이버 OnDisable에 `if(_activePad!=null){_activePad.CancelImmediate();_activePad=null;}` 류 즉시 회수 경로 있나. (TelegraphPad엔 즉시취소 API 자체가 없음 — Deactivate는 private.)

2. **★_activePad 스테일 참조 + ForceFull 오발 (이번 리뷰 H-2).** 드라이버는 `_activePad`에 *마지막 스폰 패드 1개만* 보관(2힛=최근 1개). 풀 고갈(>poolSize 동시)로 RecycleOldest가 *이 드라이버의 _activePad를 다른 드라이버에게* 재배정하면: ClawHit의 `_activePad.ForceFull()`이 *남의 패드를 가득 채운다*(엉뚱한 장판이 1로 점프). 기본값(poolSize 12·maxAttackTokens 2)에선 동시 활성 ≤4라 미발화. 노브(토큰↑·패드풀↓)로 12 초과 시 발화 — **유저 게임감 판정 항목**(요청 않은 풀확대는 CLAUDE.md 금지). ProjectilePool엔 이 클래스 없음(글롭은 발사 후 드라이버가 참조 안 들고, 자기 _active로만 산다). 패드는 드라이버가 참조 보유=새 표면.

3. **패드 풀 멤버십 = ProjectilePool과 동형이라 안전(추적으로 확인).** Acquire: idle.Dequeue 또는 RecycleOldest→live.Add. Return: live.Remove→!idle.Contains 가드 후 Enqueue. RecycleOldest는 live[0]만 떼고 직후 Acquire가 live.Add+SpawnFan/Begin이 *완전 재초기화*(style/size/progress/방향/타이머 전부 덮음) → 멤버십 1곳 유지. **"RecycleOldest가 패드를 live에 두 번 넣는다"는 의심은 Begin 재초기화 때문에 틀림** — 글롭과 같은 논리. 이중 Return도 Return의 `!idle.Contains` + Update가 Deactivate 후에만 Return(재진입 시 `if(!_active)return`)로 차단.

4. **_Progress 이중 구동은 경합 아님(추적 확인).** Update 타이머가 0→1 진행 + ClawHit→ForceFull이 1 스냅. ForceFull은 `_fillElapsed=_fillDuration`을 세팅 → 다음 Update에서 `_fillElapsed < _fillDuration` 거짓 → else(홀드) 분기로 직행, 진행값을 *다시 안 덮음*. 한 번 ForceFull이면 1에 고정 후 홀드 카운트→Return. 싸우지 않음. **단 ForceFull은 `if(!_active)return` 가드 있음** — 패드가 이미 자가 Return(홀드 끝)된 뒤 ClawHit이 늦게 오면 무음 무시(올바른 폴백, 단 장판 없이 히트만).

5. **부채꼴 전각 클램프 = 셰이더+드라이버 양쪽 방어(이중 안전).** 셰이더 ThreatArc.shader:134 `clamp(_AngleDeg,1,180)` + 드라이버 SpawnFan에서 `Mathf.Clamp(angleDeg,1,180)`(TelegraphPad.cs:78). 100° 시작값 안전. iq pie SDF는 반각>90°(전각>180°)에서 부호반전→장판 통째 소실. 둘 중 하나만 있어도 막히나 둘 다 = 견고.

6. **레이어13(PickupInfo) 공유 — 물리 간섭 점검 필요(미해결).** 장판 쿼드를 PickupInfo 레이어에 둠(콘 면제 렌더 경로). 실 게임의 PickupInfo/StrainHarvest가 *이 레이어로 트리거/레이캐스트*하면 장판 쿼드(Collider 없음)는 물리 무간섭이나, **레이어 기반 컬링/카운팅 시스템이 있으면 오염 가능**. 랩엔 픽업 시스템 부재라 미검증. 통합 시 PickupInfo 레이어 소비자(GetComponentsInLayer류) 전수 확인.

**2026-06-14(6차) OnAnimatorMove 루트모션 수동적용+AdvanceGain 1.3× 전진증폭 리뷰 — Critical 0, High 0, Medium 1(잠복·미측정 클립), Low 2. 드라이버 런타임 사고 0:**
- **★#2 이중적용 = 없음(Unity 의미 정확, 반증).** 같은 GameObject에 OnAnimatorMove 구현 시 Unity *자동* 루트모션 적용이 **콜백으로 위임**(자동 OFF)된다 — 자동+수동 둘 다 도는 2.3× 시나리오 불가. 실효 정확히 1.3×. `applyRootMotion=true` 유지가 정석(false면 deltaPosition/deltaRotation이 채워지지 않아 전진 사망). 드라이버 주석(:115-117,264-265)이 의미 정확히 설명.
- **★#1 OnAnimatorMove null/disabled 안전 = 견고(반증).** ①콜백 본문 첫줄 `if(modelAnimator==null||model==null)return`(:272) 방어 가드 있음. ②Awake가 modelAnimator null이면 `enabled=false`로 자살(:112) — disabled 컴포넌트는 OnAnimatorMove 안 불림(Unity는 enabled=false MonoBehaviour의 메시지 콜백 스킵). ③self-suicide 경로에선 어차피 가드가 null 리턴. NRE 표면 0. **단 Awake null 자살 시 applyRootMotion 설정줄(:118) 도달 못 함=무해(전진 자체가 없음).**
- **★model==Animator transform 불변식 = 프리팹 YAML로 실증(이 슬라이스 모든 안전의 토대).** Dimaxillosaurus.prefab: 루트 GO "Dimaxillosaurus"(m_GameObject 4026326723524121887)가 Transform+Animator 둘 다 소유, Animator.m_GameObject=같은 루트. 스포너 `model=enemy.transform`(루트)·`modelAnimator=GetComponentInChildren<Animator>()`(self-first→루트의 Animator) → **동일 transform 확정.** OnAnimatorMove 발화 보장(Animator 붙은 GO의 MonoBehaviour)+`model.position+=`가 올바른 transform 이동 둘 다 성립. (루트 프리팹 localPosition x:-6.58은 저작 오프셋, 런타임 스포너가 position 덮음=무해.)
- **★#6 시간 이중곱 = 없음(반증).** `model.position += deltaPosition*AdvanceGain` — deltaPosition은 *이번 프레임 애니 진행분*(animator.speed·Time 이미 반영된 누적 델타). 코드는 Time.deltaTime 추가곱 없음, 순수 ×상수. 이중 시간 0. (Roar speed 5×·구간 speed 1.9~2.5도 deltaPosition에 자동 반영 — Roar 압축재생이 전진델타도 5× 빠르게 운반, 코드 개입 0.)
- **★#7 AdvanceGain SSOT = 단일정의.** grep 전수: `public const float AdvanceGain=1.3f` 드라이버 1곳만, 빌더/캡처/스포너 desync 0. (state.speed 4const처럼 빌더가 참조하지도 않음 — 순수 런타임 드라이버 게인이라 SetupData 재빌드 불요, 컨트롤러 불변.)
- **★#5 조기탈출 중 전진 = 회귀 아님(동작클래스 동일, 스케일만).** Update가 `target==null`/`model==null`로 ReleaseToken+return해도 OnAnimatorMove는 독립 발화 → 클로 중이면 ×1.3 전진 계속. **단 이건 *이전 자동적용(×1.0)과 같은 동작 클래스*** — 자동적용 때도 Update 조기리턴과 무관하게 루트모션은 클립이 도는 한 적용됐다. 게인은 그 *크기*만 1.0→1.3. 새 사고 아님. (타깃 소실 시 Recovery→Idle 폴백 전이로 곧 Idle 도착→루트모션 0인 Idle이면 전진 멈춤 — 단 아래 M-1 전제.)
- **★#4 deltaRotation×FaceTarget = 무해(클로 클립 회전 0 실측).** OnAnimatorMove가 `model.rotation *= deltaRotation`(:274)을 Update의 FaceTarget(:179 직접 rotation 셋) *뒤에* 곱함. 클로 _RM 실측 측면0·상승0이고 회전 루트모션도 0(≈identity)이라 FaceTarget 조향 보존. **단 이것도 "클립 회전 0" 측정 전제** — 만약 클립에 yaw 루트모션이 있으면 FaceTarget 조준 위에 곱해져 궤적 휨. 클로는 실측됐으나 Idle/Roar는 미측정(M-1과 같은 뿌리).
- **M-1(이번 유일, 잠복): Idle/Roar 루트모션 미측정 → 비-0이면 1.3× 증폭(#3 핵심 우려, 코드로 반증 불가).** OnAnimatorMove는 *모든 상태*의 deltaPosition을 ×1.3 — 클로뿐 아니라 Idle/Roar 포함. 클로 클립만 실측(측면0·상승0·전진2.218m). **Idle/Roar는 셋업이 측정한 적 없고**, 소스 FBX(`Dimaxillosaurus@Idle/Roar.fbx`)는 `clipAnimations:[]`(default import)·`animationType:2`(Generic)·`rootMotionBoneName:root`·Bake-Into-Pose 없음 → **root 본의 baked 변위가 그대로 루트모션으로 통과**. Idle/Roar 제작 시 root에 translation이 구워져 있으면 그 드리프트가 이제 ×1.3로 증폭된다(평시 Idle 대기 중 몬스터가 슬금슬금 밀리거나, Roar 5×압축이라 짧은 시간에 확 밀림). 디스크로 측정 불가(런타임 SampleAnimation 필요). **픽스 후보: ①Idle/Roar SampleAnimation 실측해 0 확인(권장), ②또는 게인을 클로 상태에서만 적용(`if(inClawState)`)해 Idle/Roar는 1.0 강제.** 발 미끄럼(게임감)이 아니라 *제자리 상태가 미세 전진/회전*하는 위치버그 가능성이라 잡음. 클로는 안전.
- **L: deltaRotation 증폭 비대칭(설계상 OK, 기록).** position은 ×1.3, rotation은 ×1.0(주석 :269 "클립 회전 0이라 ≈identity"). 의도적(거리만↑, 타이밍/방향 보존)이고 클로 회전0이면 무해. 단 M-1대로 Idle/Roar 회전루트모션이 비-0이면 그건 1.0이라 증폭 안 됨=position만 증폭되는 비대칭 — Idle 드리프트가 회전 없이 직선으로만 1.3× 밀림. M-1 해소되면 무의미.
- **L: AdvanceGain Mathf 클램프/음수가드 없음.** const 1.3 하드코딩이라 현재 무관하나, 주석(:100)이 "유저 ▶ 1.2~1.4 튜닝" 노브로 명시 → 유저가 음수/0 넣으면 후진/정지(런타임 NRE는 아님, 게임감 파탄). const라 컴파일타임 고정=Low. 노브를 SerializeField로 승격 시 `[Min(0f)]` 권고.

**2026-06-14(5차) 단발 4분할 SPLIT(Swing/Recovery 2분할 → Windup/Strike/FollowOut/Recovery 4분할, 10상태/8해시, 구간별 정적 speed 이즈램프 1.9/1.35/2.3/2.5) 리뷰 — Critical 0, High 0, Medium 2(둘 다 잠복·게임감), Low 2:**
- **★프롬프트 핵심 우려 "ExitTime 전이 드랍으로 상태 갇힘" = 반증(중요 교훈).** Unity exitTime 전이는 **레벨 트리거** — 비루프 상태에서 normalizedTime이 exitTime(0.99)을 *넘어서기만* 하면 발화(정확히 [0.99,1.0] 착지 불요). 한 프레임에 0.5→1.4 점프해도 `>=0.99` 참이라 전이됨 → 가장 짧은 FollowOut(6프레임/2.3=~0.087s, 30fps~2.6틱)에서도 저프레임/스파이크로 안 갇힘. **드랍되는 건 *전이*가 아니라 *AnimationEvent*다** — 비루프 상태가 한 프레임에 1.0 초과 시 그 상태의 이벤트 미발화 케이스 존재. → ★M-1: FollowOut 0.087s는 지금 드라이버 분기 no-op이라 무해하나, 향후 이 구간에 이벤트/로직 달면 저프레임 무음 누락. "FollowOut/Recovery=순수 시각, 이벤트 금지" 못박기 권고.
- **★캡처 watcher rename 동반 갱신 = 직전 C-1 회귀 안 함(메모리 교훈 적용 확인).** 4차 C-1(상태 rename 시 `IsName` 미갱신→`_shotMask` 영원 미완→완료게이트 `==0x1F` 영원거짓→무한폴링 스톨)이 이번엔 발생 0. watcher IsName 5개(Roar/L·R Windup/Strike)가 빌더 AddState 이름과 100% 일치(grep로 옛 Swing/ClawLeft IsName 잔존 0 확인, 주석에만). 완료게이트 0x1F(5비트)↔Shot 5콜 정합. ★"상태 rename=watcher IsName 동반 갱신 필수"가 4차→5차에 지켜짐.
- **★콤보 셋업 1회성 = 정확(8상태 추적).** Update 8상태를 Windup(2, 셋업 `if(!_comboEntered)` :220) / Strike·FollowOut·Recovery(6, 순수 no-op :237-242)로 커버. `_nextRight=(hash==SLWindup)` :229는 *새 Windup 해시*로 판정(옛 SLSwing/SRSwing 해시 잔존 0). 셋업은 Windup 진입 1회뿐 — 나머지 6상태 무동작이라 재실행/누락 불가.
- **★SSOT 4const = desync 불가.** 드라이버 public const 4개(:86-89)↔빌더 `const X=DimaxillosaurusBrawler.X` 4개(:58-61) 크로스참조, 이중정의 0. 2차 ClawSpeed 이중const 풋건이 4const에 처음부터 SSOT 적용.
- **★ClawHit norm 재계산(풀클립→Strike-clip 기준) = 산술 정확, 경계 안전.** L=(12.25-9)/(16-9)=3.25/7≈0.464, R=(12.845-9)/7≈0.549 둘 다 [0,1]. 컨택 절대frame이 Strike윈도[9,16] 안 실재→히트 진짜 타격포즈. ClawHit Strike에만(:155), Follow/Recov `new AnimationEvent[0]`=중복0. **단 분모(StrikeFrame-WindupFrame) 0가드·firstFrame<lastFrame 역전가드 없음** — 상수 단조증가(9/16/22/35)라 현재 안전, 에디터 빌드스크립트라 런타임 아님(L 미만).
- **M-2: CUT 경계 3개로 늘어난 루트 연속성(4차 M-1 승계·확대).** 2분할=경계1(frame22)→4분할=경계3(9/16/22). `keepOriginalPositionXZ=false`라 firstFrame≠0 sub-clip은 루트XZ가 그 클립 첫프레임 상대 리베이크. per-strike 총전진 보존이나 CUT 경계 루트/yaw 스냅을 코드/빌더검증(:163-176 길이·이벤트개수만)이 증명 안 함. 픽스=빌더에 경계 rootT 델타 로그 or 육안1회. 게임감=유저 ▶.
- **L: ClawHit 빈스텁(:249-252) 유지** — 통합 시 Strike-clip 기준 norm으로 히트 박히는지 재확인(추적상 norm 정확). **L: 캡처 Strike윈도 폭0.1(~1틱폭)** — 컨택norm 윈도 안이나 스파이크1틱 건너뛰면 그 컷만 누락(완료게이트 영원거짓→캡처 에디터 스톨, 런타임 아님). 윈도 [0.35,0.65]로 넓히면 마진.
- **★4차 M(OnDisable `_firedThisIdle` 리셋 누락 소프트락) FIXED 유지** — OnDisable→ReleaseToken→ResetCombatState가 `_firedThisIdle=false` 포함(:129). 4분할로 안 깨짐.

**2026-06-14(4차) 단발 Swing/Recovery SPLIT(1상태→Swing자연1.0+Recovery배속3.0 2상태, 6상태/4해시) 리뷰 — ★Critical 1(캡처 댕글링 회귀), Medium 1, Low 2. 런타임 드라이버는 Critical/High 0:**
- **★C-1 = 댕글링 상태 참조가 *캡처 파일*에서 회귀(이번 ★1 점검 항목 실발화).** split가 상태를 `ClawLeft/ClawRight`→`LeftClaw_Swing/Recovery·RightClaw_Swing/Recovery`로 rename했으나 `DimaxillosaurusLabCapture.cs:145-152`는 옛 `IsName("ClawLeft"/"ClawRight")` 유지. `IsName(존재안하는이름)`=무음 false → 비트2/4/8/16 영영 미세트 → `_shotMask` 최대 0x01 고정 → 완료게이트 `==0x1F`(:154) 영원거짓 → `-=Watch` 도달불가(watcher 플레이종료까지 무한폴링) + 5컷 중 4컷 미저장. **3차 메모 "Capture 정합"은 *그때 상태명* 기준 — rename이 깨뜨림.** 무음(컴파일·"무장"로그 정상)이라 안 잡으면 모름. ★교훈: **상태 rename = Capture watcher의 IsName/shortNameHash 비교 동반 갱신 필수(회귀 핫스팟)**, 빌더 로그 기대 상태명(:287)을 watcher와 대조. 픽스=Swing만 컨택/채움 잡기(Recovery 0.144s라 윈도 협소), 스윙기준 컨택 norm L≈0.557/R≈0.584(:64-65) 사용. 주석 라벨 "norm 0.350/0.367"도 *풀클립* 기준이라 split 후 스윙기준 갱신.
- **★게이팅(2상태 1단발) = 정확(추적 반증).** 콤보셋업 Swing진입 `if(!_comboEntered)`(:208)만 1회, `_nextRight=(hash==SLSwing)`(:217) 진입손 파생 → **단발당 1회**(Recovery 브랜치 :224-227 무동작=이중토글 불가). `_comboEntered` Idle(:167)만 클리어. chain 발사 Idle `_engaged`+`_gapTimer<=0`+`_firedThisIdle`(:188-192) → **Recovery는 chain 안 쏨=Recovery 스킵 불가/Swing-end 조기발사 불가.**
- **★sub-clip 분할 정합 = 정확.** 같은 take 2 ClipDef(swing 0~22 / recov 22~35, :145-147), 컨트롤러 `LoadClipByName`(:201-204) null이면 빌드중단(:208). ClawHit Swing에만(:145)·Recovery `new AnimationEvent[0]`(:146)=중복0. **이벤트 정규화 정확**: 컨택 절대frame12.25/SplitFrame22=0.557 ×스윙0.733s=0.408s(원본초 보존, importer 정규화함정 :58 회피).
- **M-1(잠복): Recovery 루트 baseline 연속성 미검증(★5).** `firstFrame=22` sub-clip은 importer가 로컬0 리베이크 → `keepOriginalPositionXZ=false`(:180)라 루트XZ가 *클립 첫프레임 상대* 베이크. per-strike 총전진(2.218m)은 보존(주석 :39 맞음)이나 **Swing→Recovery CUT 경계 루트/yaw 스냅 가능성을 코드/이벤트가 증명 안 함**(빌더 재로드 검증 :153-163은 길이·이벤트개수만, 루트연속성 X). 같은 take·동일 `keepOriginal*` 플래그라 대개 매끄럽지만 "코드로 증명 안 된 가정". 픽스=빌더에 루트연속성 로그 1줄 or 플레이 1회 육안(0.733s 지점 루트 툭 튀나). 게임감=유저 ▶.
- **★4 RecoverySpeed SSOT = 정확.** 드라이버 public const(:77)→빌더 참조(:56)→state.speed(:238,243). 2차 ClawSpeed 이중const 풋건을 RecoverySpeed엔 처음부터 SSOT 적용=desync 불가. Recovery 배속은 *state* speed라 `animator.speed=1`(:159) 매프레임리셋과 곱무충돌(Roar 5× 동일).
- **★6 승계 무결.** ResetCombatState(:112-125) _firedThisIdle 포함 6플래그+트리거3 청소, static Roster ClearStaticState, 토큰 비게이팅(:175 best-effort/:176 무조건 SetTrigger)·null가드 전경로 ReleaseToken. split가 이전 5회 통과분 안 깸. **L: ClawHit 빈스텁(:234)=통합시 데미지0 무음(유지).**

**2026-06-14(3차) "벽처럼 오는 클로월" 전면 재작성(거리게이트 전체 제거·Approach/run/walk 상태 통째 제거·장판 텔레그래프 Dimax 전용 제거·토큰 비게이팅화) 리뷰 — Critical 0, High 0, Medium 1, Low 2(★댕글링·★즉시포효 재발사 둘 다 코드추적 반증):**
- **★댕글링/스테일 참조 = 0 (이번 최우선, 완전 반증).** 제거 심볼(isMoving/isRunning/attackRange/breakRange/runDistance/moveSpeed/telegraphPool/TelegraphPool/TelegraphClaw/SpawnFan/_activePad/Approach) grep — 드라이버·빌더·스포너 전부 *주석에서만* 등장(제거 설명), 라이브 코드 0. SetBool/SetFloat 드라이버에 아예 없음(SetTrigger만). GetCurrentAnimatorStateInfo 비교 4개(Idle/Roar/ClawL/ClawR) 전부 빌더 생성 상태와 일치=영원거짓 비교 없음. **Capture 사이드파일(DimaxillosaurusLabCapture.cs)도 정합** — Roar/ClawLeft/ClawRight만 watch, 옛 Approach/isMoving·TelegraphPool 와이어링 잔존 0. 외부 파일 `.attackRange` 등 setter 0(스포너는 model/modelAnimator/target/attackController/tokenPool만 set). **컴파일 통과+런타임 무효호출도 0.**
- **★즉시포효 1회 게이팅 = 견고(프레임추적 반증).** Idle `!_engaged` 경로: `_firedThisIdle` 가드로 1회만 SetTrigger(PAttack). F0 발사→F1(아직 Idle, _engaged 여전 false지만 _firedThisIdle=true)→스킵. _engaged는 *클로 진입에서만* true→포효 후 Idle 복귀 땐 항상 else(chain)분기=`!_engaged` 영영 안 탐. **포효 정확히 평생 1회, 머신건 로어 불가.** Roar분기 `_firedThisIdle=false` 재무장은 *미래 Idle*용이나 그땐 _engaged=true라 무해.
- **★무한 L→R→L→R = 자가파생 무한(추적 반증).** `_nextRight`가 토글 아니라 클로 진입 시 `hash==SClawL`에서 파생 → 실제 친 손 반대를 다음으로. gap 비트(_gapTimer) 동안 FaceTarget 회전만+decrement, 끝나면 삼항 택1 발사(_firedThisIdle로 1프레임지연 더블파이어 차단). 데드락·한손고착 불가. ResetCombatState가 attack/chainL/chainR 셋 다 ResetTrigger=스테일 스턱 0.
- **★이전 2차 M(`_firedThisIdle` OnDisable 리셋 누락 소프트락) = FIXED.** 현 OnDisable→ReleaseToken→ResetCombatState가 `_firedThisIdle=false` 포함(:103). disable→re-enable 영구동결 닫힘.
- **★토큰 비게이팅 정합(반증).** 토큰 Acquire = Idle `!_engaged` 오프너 1회 best-effort(`!_holdsToken && tokenPool!=null && TryAcquire()`), 못 잡아도 SetTrigger(PAttack) 무조건 진행=벽 안 멈춤. Release = OnDisable/null가드(target·model·modelAnimator) 전경로 ReleaseToken→ResetCombatState. "_holdsToken⟺교전수명" 불변식 유지(누수·언더플로우 0, AttackTokenPool.Release `_inUse>0` 가드). **단 이전 슬라이스 L-footgun "tokenPool==null이면 영영 공격 안 함"은 이번 재작성에서 *해소됨*** — 토큰획득과 SetTrigger 분리(획득 실패/풀null이어도 포효 발사). 비게이팅 설계 정확.
- **M(이번 유일, 잠복): chainGap=0 이면 단발 사이 회전 비트 소실 → 직선 클로질만(사이드스텝 추적 빵꾸).** 드라이버 주석이 인지("0이면 직선만")하나 코드 미강제. gap=0이면 ClawX→Idle 진입 프레임에 즉시 `_gapTimer<=0`+`!_firedThisIdle`→다음 트리거 즉발=Idle서 FaceTarget 1프레임만 회전. 플레이어가 측면 대시하면 벽이 못 따라 틂. **게임감(추적성) 항목=유저 ▶, 코드결함 아님.** Min(0f) 클램프도 없음(음수 gap 넣으면 항상 즉발=동일).
- **L: ClawHit AnimationEvent 핸들러 = 빈 스텁(데미지 미연결).** :213 `ClawHit(AnimationEvent ev)` 본문 비어있음(향후 데미지 훅 자리). 랩=비주얼 검증용이라 의도된 공백이나, *통합 시 이거 안 채우면 클로월이 데미지 0*(무음 — 닿아도 안 아픔). DontRequireReceiver라 미수신도 무에러. 통합 체크리스트 항목.
- **L: 빌더-드라이버 ClawSpeed 단일 const화 = 2차 M 해소.** DimaxillosaurusLabSetup.cs:35 `const float ClawSpeed = DimaxillosaurusBrawler.ClawSpeed` (드라이버 public const 참조). 2차 리뷰의 "이중 const desync 풋건"이 SSOT로 통합됨(드라이버가 단일 진실원). 잔존 위험 0.

**2026-06-14(2차) 좌우 단발 교대 재설계(전진 2힛 콤보 1상태 → ClawLeft/ClawRight 2상태 L→R→L→R, state.speed 1.45) 리뷰 — Critical 0, High 0, Medium 1(코드 추적 전부 반증):**
- **★옛 단일 `chain` 트리거 → `chainL`+`chainR` 분리 = 완벽 대칭(이번 최우선 의심, 반증).** 빌더 AddParameter/AddCondition 문자열·드라이버 해시(PChainL/PChainR)·*모든* ResetTrigger 사이트(ResetCombatState/Roar재무장/ClawEntry) 빠짐없이 둘 다 청소. 옛 `chain`/PChain 잔존 0(grep). attack/chainL/chainR 셋 다 `_firedThisIdle` 가드 발사. 스테일 트리거 스턱 불가. **이전 슬라이스의 1프레임 지연+청소누락 함정을 분리 후에도 유지.**
- **★좌우 교대(`_nextRight`) = 자가치유(반증).** 토글 아니라 클로 진입 시 `info.shortNameHash`에서 *파생*(SClawL→다음R). 한 Idle 프레임 chainL/R 동시발사 불가(삼항 택1). escape/death/re-engage 후 ResetCombatState→`_nextRight=false`+`_engaged=false`→Roar오프너→ClawLeft고정→재파생. **stale여도 최악 손1회 어긋남, 진입 즉시 실제 친 손에서 교대 재개=영구꼬임 불가.** ResetCombatState가 `_nextRight`도 리셋(비대칭 차단).
- **★배속 fill 보정 수학 정확(반증).** fillDuration=nativeFill/ClawSpeed가 1.45× 재생 하 스폰→컨택 wall-clock 갭과 일치(0.2495s, 자릿수까지). ForceFull=하드스냅 아닌 확정동기(패드가 컨택 프레임 자연 ~1.0 도달). **단 매칭 전제.**
- **★단발 cadence = 콤보보다 *적은* 동시패드.** 단발당 1패드(수명 ~0.37s=fill0.25+hold0.12), 2어태커×~0.985s주기→동시≤2, poolSize12 한참 밑. RecycleOldest 압력0→gen-stale `_activePad` 오발(H-2) 기본값 휴면.
- **M(이번 유일): ClawSpeed 이중 const = 무음 desync 풋건.** DimaxillosaurusBrawler.cs:102 + DimaxillosaurusLabSetup.cs:39 양쪽 `const 1.45`. 빌더=state.speed 굽기, 드라이버=fill/ClawSpeed. **일치할 때만 컨택정합.** "유저 ▶ 노브"라 손댈 확률↑, 한쪽만 튜닝 시 채움-컨택 어긋남(드라이버만↑=ForceFull무의미 거짓장판/빌더만↑=컨택때 미완 하드스냅). 무음(컴파일·로그 정상). Critical 아님=기본값 일치+양쪽 교차참조 주석. 픽스=빌더 BuildController 로그(:279)에 드라이버 값 환기 한 줄(비파괴) or SSOT 통합(드라이버 public const을 빌더가 읽기, 유저 승인 하).
- 캡처 워처 EditorApplication.update: play-only 가드+완료/play-exit 해제+재무장 `-=후+=` 이중구독 차단. 에디트오염·구독누수 0.
- **이전 슬라이스 H-1/H-2 픽스 생존**: TelegraphPad에 `CancelImmediate(int gen)`/`ForceFull(int gen)`/`Gen` API 추가됨(예전 메모 "즉시취소 API 없음"은 stale). OnDisable `_activePad.CancelImmediate(_activeGen)` gen-guard 미히트 취소. `_activePad` 수명=단발1사이클(스폰→ClawHit null) 깔끔, 누수0.

**2026-06-14 카타나 슬래시 궤적 VFX(SlashVfxPool/SlashVfxFX/KatanaController 재배선) 리뷰 — ★Critical 1, High 2, Medium 3, Low 6. ★art "머티리얼 누수 없음" 주장=3종 전부 검증통과(정확), 결함은 art가 검증범위 밖에 둔 *싱글톤 수명*과 *스파크 _life 누락*에 몰림:**
- **★★C-1 자가부트스트랩 싱글톤 + 호출자 stale 캐시 = SmashImpactPool 안전망 상실(이 슬라이스의 진짜 새 위험).** SmashImpactPool/TelegraphPool은 **스포너 주입**(매 스폰 참조 전달)이라 stale 캐시 자체가 없음. SlashVfxPool은 *자가부트스트랩 싱글톤*(NoiseManager식 `Instance` getter→없으면 auto GO 생성)으로 바꾸면서 그 대가인 fake-null 재확인 책임이 호출자(KatanaController)로 넘어왔는데 **안 함**. KatanaController.cs:185-193 `_slashVfxResolved` 영구 캐시(`if(!_slashVfxResolved){_slashVfx=Instance;_resolved=true;}`)→한 번 resolve하면 영구보관. ★SlashVfxPool은 **DDOL 미호출**(grep: SlashVfxPool.cs DDOL 0건 — 이 프로젝트 타 self-bootstrap GlitchFX/AmbientBedDrone/MetaProgress는 전부 DDOL 호출, SlashVfxPool만 빠짐)=씬 종속. 씬 리로드(LoadScene 런재시작)에서 풀이 KatanaController보다 먼저 죽으면 `SlashVfx?.Acquire()`의 `?.`가 Unity fake-null 잡아 **무음 VFX 소실**(art 보장 "항상 보임" 거짓화, NRE는 아님). 자가치유 경로(PlayerCombat 씬종속→씬리로드 시 KatanaController도 new로 재생성→_resolved=false)는 있으나 *풀만 죽고 KatanaController 생존* 경로(외부 Destroy·멀티풀레이스)에선 깨짐. **픽스=_slashVfxResolved 플래그 제거 + getter에서 `if(_slashVfx==null) _slashVfx=Instance;` 매번 fake-null 재확인**(SmashImpactPool은 주입이라 이 함정 회피했음 — self-bootstrap 전환 순간 호출자 책임으로 이전됨).
- **★H-1 스파크 PS가 `_life` 계산에서 누락 = 쌍둥이 SmashImpactFX가 옳게 한 처리를 빠뜨림.** SlashVfxFX.cs:375 `_life=Mathf.Max(_slashLife,_rangeLife)+0.05f` — 스파크 미포함. 스파크 main.duration0.4+startLifetime최대0.28≈0.68s인데 PlayWave _life=0.45s→**스파크 살아있는데 먼저 Return**(:404-408). Return시 `_spark.Stop(StopEmitting)`(Clear 아님)→방출된 파티클 안 지움→idle 큐서 잔류 스파크 허공에 뜸(World 시뮬공간 위치고정) or 다음 Acquire의 `_spark.Clear(true)`(:367)가 *뚝* 지움(팝). **SmashImpactFX.cs:238-240은 dustLife를 _life에 포함**(쌍둥이 정답). 픽스=BeginLife에 `sparkLife=_spark!=null?(_spark.main.duration+_spark.main.startLifetime.constantMax):0f` 포함(한 줄).
- **★H-2 멀티풀 레이스 + InitPoolSize dead API + Awake 자살가드 충돌.** Instance getter(:40-53) 단일스레드 Unity선 AddComponent 동기Awake로 실질 안전하나, 주석은 "스포너 주입 대상 아님"인데 `InitPoolSize`(:69 SmashImpactPool 복붙 잔재)가 있고 *호출자 0건*(grep: KatanaController 안 부름)=dead. InitPoolSize 규약(inactive GO에 AddComponent후 SetActive)을 쓰면 Awake 자살가드(:57)와 충돌(외부 주입 풀을 자살). 현 단일호출자라 미발화. 픽스=self-bootstrap 전용 명시 or InitPoolSize 제거.
- **★머티리얼 누수 3종 전부 무죄(art 주장 검증통과).** ①슬래시: SetSlashMaterial(:282-290)이 매 Play `if(_slashMat!=null)Destroy` 후 new→동시 1개, OnDestroy(:414) 마지막 1개. ②범위: Build 1회생성(:106)·OnDestroy(:415). ③스파크: ConfigureSpark 1회(:165)·OnDestroy `pr.sharedMaterial` 파괴(:418-419). 인스턴스당 각 1개·누수 0. Destroy 프레임지연도 sharedMaterial 즉시교체라 죽은 머티리얼 렌더참조 프레임 없음. _quadMesh=내장 sharedMesh라 Destroy 안 함(정확). 셰이더 null·레이어 미존재 폴백 견고(_hasRange 게이팅, SmashImpactFX 규약 일관).
- **M-1 `_waveArc` LineRenderer 중복렌더(게임감 유저▶).** "ShowTrailArc/Line 호출제거, _waveArc 보존"인데 _waveArc는 단순보존 아니라 UpdateVisuals서 _waveActive동안 매프레임 DrawWaveArc(:699-704, rq3100) 실그림 + PlayWave ThreatArc 확장부채꼴(SlashVfxFX:274, rq3000)=참격파 시전중 외곽선+범위SDF 이중. art "자작 트레일 대체" 주석과 모순. _footRing(발밑게이지)은 역할달라 중복아님(보존정당). 의도면 기록, 대체면 제거.
- **M-2 dead code orphan(이번 재배선이 만든 것=정리대상).** ShowTrailArc/ShowTrailLine(:749-779) 호출0 + _slashTrail 필드는 생성(:212)·매프레임 페이드(:683-696)·Cleanup파괴(:816) 잔존하나 _trailTimer 세팅처(ShowTrail*)가 제거돼 enabled 영원false=GO+Material 낭비. CLAUDE.md "당신 변경이 만든 orphan 정리" 해당. grep으로 타 소비자 확인 후 _slashTrail/ShowTrail*/_trailTimer/TrailHold/페이드블록 동반제거.
- **M-3 poolSize6 고갈시 RecycleOldest=진행중 궤적 뚝(게임감).** 현 cadence(참격평타0.55,5단-60%=0.22s, 수명0.26s)로 동시~2개라 6 여유, 단 FireRateMult 메타강화가 cadence 더 줄이면 발화. poolSize 유저노브 유지+모니터링.

**2026-06-14(1차) 전진 압박형 재설계(제자리 콤보→전진 콤보 체인) 리뷰 — Critical 0, Medium 1:**
- **★Animator Trigger 체인 수명 = 이 드라이버가 새로 방어한 표면(클린).** 체인 로직(attack/chain 둘 다 Trigger)의 1프레임 전이 지연 함정을 `_firedThisIdle` 가드 + Combo진입 `ResetTrigger(attack/chain)` 대칭 + Roar진입 `ResetTrigger(chain)`로 빈틈없이 막음. 추적 검증: ①attack(_engaged=false)·chain(_engaged=true)는 ternary라 동시세트 불가 ②CUT 전이(dur0)라 1프레임 내 상태전환→다음 Update는 새 상태 분기 ③Idle 트리거 set 프레임은 항상 `SetMoving(false)` 선행→Idle→Approach(isMoving) 우선전이 안 뺏김(전이배열순서 Approach>Roar>Combo지만 isMoving=false라 통과). 더블파이어·stale chain into Combo·attack↔chain 엉킴 전부 반증됨.
- **★Combo는 비중단(non-interruptible) = 패드 overwrite-leak 원천 차단.** AnyState 전이 없음, Combo IN=Idle/Roar뿐 OUT=Combo→Idle(ExitTime0.98)뿐. 콤보 도중 chain/attack 못 쏨(Idle 전용 전이). → `_activePad` 미히트 상태로 새 콤보가 덮어쓰는 시나리오 불가(중단은 OnDisable死만, CancelImmediate가 처리). 2힛 컨택(0.217/0.633) 모두 0.98 이전이라 ForceFull→null 완결, 콤보 끝엔 항상 `_activePad=null`. gen가드/CancelImmediate(이전 슬라이스 H-1/H-2 픽스) 체인 하에서도 유지.
- **M(이번 유일): `_firedThisIdle` OnDisable 리셋 누락 = disable→re-enable 시 영구 소프트락.** OnDisable(:117)이 `_engaged/_comboEntered/_gapTimer`는 리셋하나 `_firedThisIdle`는 빠짐. SetTrigger(:214) 후 Roar분기 클리어(:248) 전 1~2프레임 창에서 disable되면 `_firedThisIdle` stale=true로 잔존→재활성 후 Idle서 사거리 안인데 트리거 영영 안 쏨(Approach/Roar/Combo 어디도 안 감→리셋 기회 0)=공격 못하는 동결. 토큰은 OnDisable ReleaseToken으로 반납돼 기아는 아님(자기만 멈춤). 랩은 Instantiate 1회라 미발화, 스포너 풀 재사용 도입 시 발화. 픽스=OnDisable 리셋목록에 `_firedThisIdle=false` 한 줄 추가(다른 플래그와 대칭).
- **L: tokenPool==null이면 영영 공격 안 함(무음).** :208 `_holdsToken || (tokenPool!=null && TryAcquire())` — 풀 null이면 둘 다 거짓→Idle 영구대기. 풀=옵션 스로틀 의도면 null=무제한이어야 하나 코드는 null=금지(반전). 랩 항상 주입이라 미발화. 거리밴드도 attackRange≤runDistance≤breakRange 불변식 무검증(설계자 역전 시 밴드 사망) — 둘 다 Low footgun.

**검증된 안전 전제(재확인 불요 — 라인 갱신 2026-06-14):**
- 토큰 대칭: DimaxillosaurusBrawler.cs:183 `if(target==null||model==null){ReleaseToken();return;}` + :181 modelAnimator null도 ReleaseToken = Venodonte 누수 픽스 정확 승계. OnDisable(:116) ReleaseToken + _holdsToken 가드 + AttackTokenPool.Release의 `_inUse>0` 언더플로우 방어. ReleaseToken이 `_engaged=false`까지 묶어 "_engaged⟺토큰보유" 불변식 유지(breakRange탈출:226·null가드 전경로). gap 조기리턴(:195) 중에도 null가드(:181-183) 선행이라 타깃소실 시 반납됨(gap 동안 거리 재평가 안 해 ~0.18s 토큰 홀드지연은 bounded, 누수 아님). 파괴·비활성·완결·타깃소실 전경로 토큰 누수 0.
- Roster static: ClearStaticState(SubsystemRegistration)로 도메인 리로드 off 잔존 방어(Caniathrox 패턴).
- 셰이더 미발견·레이어 미존재 폴백: TelegraphPool.Build가 Shader.Find null→enabled=false, 레이어<0→Default+경고. 무NRE.
- 머티리얼 인스턴스 정리: TelegraphPad.OnDestroy(:180) `Destroy(_mat)` — 인스턴스 머티리얼 누수 방어.
- 0벡터 forward 폴백: TelegraphClaw(:234)·PlaceOrientedToForward(:125) 양쪽 `sqrMagnitude<0.0001f→Vector3.forward`. 원점 겹침 안전.
- Begin의 fillDuration Mathf.Max(0.01f) + holdAfterFull Mathf.Max(0f) = 0·음수 방어(0 나눗셈 없음).
- 스폰 SetActive(false)→와이어링→SetActive(true) 순서(랩 스포너:87-100) — Awake 조기 자살 방어(Caniathrox/Venodonte 동일).
- modelAnimator.speed 단일 리셋(:158) + Approach 브랜치만 배율 — 이탈 경로 배율 누수 0. Roar는 *state* speed(5×)라 animator.speed=1 리셋과 무충돌(곱).
[[projectile-pool-pattern]] [[zombiecrush-review-hotspots]]
