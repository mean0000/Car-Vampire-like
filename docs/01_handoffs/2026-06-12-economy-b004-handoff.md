# 핸드오프: 2026-06-12 — E-002 경제 본편(게이트 대기) + B-004 스펙 동결 + 로스터 정비

> 권위: `00_authority/2026-06-12-e002-settlement-spec.md`(경제), `00_authority/2026-06-12-b004-rapidfire-spec.md`(연사), 나침반 v1.1 §4.1(정체성), 렉시콘 v1.8 §5.1(공제 어휘).
> ⚠️워킹트리에 **병렬 세션 작업물 잔존**(RearThreatHint 3종+셰이더/ReturnWaveTrigger TTC 가드/ZombieController·ZombieLKPSilhouette/폰트 에셋/agent-memory 다수) — 그쪽 소유, 이 세션 커밋에서 제외함.

## 이 세션이 한 것

1. **E-002 "정산서가 수지를 말한다" 구현 완료 — ★게이트 미통과 상태로 커밋됨.** 납품→일당→공제→실수령(현금 잔고 MetaSaveData.cash 신설), 사망=산재 금액 보전 30%/소독=미인정 0%, 작업코드 OP-#### 헤더, 업그레이드 현금 구매(Lv1 70,000/+30,000). 유저 판정 8건 동결(스펙 문서 참조 — 4번 항목 고정 골격이 핵심: 폭로 캐넌 전제). Stab+Codex 리뷰 통과(블로커/CRITICAL 0).
2. **정체성 판정 영구화**: 나침반 v1.1 §4.1 — 명사=익스트랙션 슈터(정보+판돈), 뱀서=다이얼 도착지. 가드레일: 생존=익스트랙션식(허기류 백로그 진입 금지)/스토리=구조재.
3. **B-004 Elaboration 동결**: 가설 개정 — "히트스탑·머즐 채움" 폐기(개체 히트스탑·경직 사다리 이미 구현돼 있었음) → **"세어지는 트랜지언트의 행렬"**. 코어 4채널(트레이서 4:1/머즐 변조+광원 스트로브/사운드 행렬+명중 thud/넉백 누적+그레이즈 노브화). 탄피 카펫·이동 제약은 제외(유저 판정).
4. **로스터 정비**: `Sound` 에이전트 신설(별칭 `sd`, 못 듣는 한계=제0원칙 내장, **다음 세션부터 로드**), gameplay.md 모델 fable 고정, Stab을 Unity 위험 카탈로그(사고 이력 8계열)로 재조준, 보류 에이전트 3종+신설 트리거 메모리 기록.

## ★다음 큐 (순서 고정)

1. **E-002 게이트 (유저 플레이)** — ①탈출 정산서→업그레이드 구매 ②일부러 사망("보전 +36,000에도 실수령 0"이 아픈가/웃긴가) ③선택: 타이머 만료(산재 미인정). 게이트 질문: **"정산서 받고 '더 벌러 가야겠다'가 생기는가?"** 미달 시 RunConfig 노브(단가 10,000/장비 30,000/보험 20,000/보전 0.3)로 즉석 연장.
2. **통과 시 B-004 착공** — 가드 2개: ①ZombieController/StrainHarvestFX = FX 세션 파일권, 착공 전 커밋 확인(미커밋이면 C4는 CombatFeelConfig 노브만) ②C3 사운드는 Sound 에이전트 첫 실전.
3. 게이트 후 볼트 로그 1줄 + E-002 메모리의 "게이트 대기" 해제.

## 미결

- **앰비언트 12마리 = 조건부 종결**: 현 씬(Greybox_CombatLab) 스폰 경로 없음(스포너 GO 비활성). 유력 가설 ①ScanLit_v2 스포너 활성(그 씬은 minZombies=15까지 채움) ②계수 착시(랩7+웨이브4=11). E-002 게이트 플레이에서 재발 시 스폰 지점 1줄 로그 계측. ScanLit_v2 스포너 끌지는 유저 콜.
- 리뷰 잔여 참고(수정 안 함): 업그레이드 구매 시 이중 Save I/O(무해), ReturnToOffice의 buildIndex<0 경로 timeScale 잠김(기존 이슈), SettlementReport struct의 Dictionary 얕은 복사(향후 로그 저장 시 함정).
- 보급 UI 어휘(장비 불출/보급 투하) 렉시콘 판정 유보 — 보급 UI 만들 때.
- Signal 좀비 HP 20 밸런스 / GateLab_Corner 정리 여부 (06-11분 이월).

## 함정 기록 (재발 금지)

- **백그라운드 에이전트는 권한 프롬프트를 못 띄운다** — 파일 수정 작업은 반드시 포그라운드로 위임(E-002 1차 에이전트가 분석만 하고 중단된 원인).
- **Explore 보고의 좌표는 로컬/월드 검증 필수** — 씬 YAML의 m_LocalPosition을 월드로 오독해 "랩 좀비 7=앰비언트" 오판 직전까지 감(랩 루트 x≈60 오프셋).
- **같은 Run/ 폴더라도 파일권은 세션별** — ReturnWaveTrigger.cs가 어느새 콘 세션 소유가 됨(TTC 가드+미커밋 RearThreatHint 참조). 커밋 전 의심 파일은 `git diff`로 직접 내용 확인 — 오늘 혼입 시 빌드 파손이었음.
- 3세션 병렬의 메모리/문서 동시 갱신 — MEMORY.md가 세션 중 외부 변경됨. 커밋·문서 수정 전 최신 상태 재확인.
