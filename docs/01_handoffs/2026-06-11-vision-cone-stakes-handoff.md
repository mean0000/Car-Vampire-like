# 핸드오프: 2026-06-11 밤 — 시야 콘 정보 게이트 + 모퉁이 랩 + E-001 판돈 (게이트 1차 통과)

> 권위: `00_authority/2026-06-11-corner-lab-spec.md`(랩), `00_authority/2026-06-11-e001-stakes-bolton-spec.md`(E-001)
> 커밋: `e972a3dc0`(이 세션 전체). ⚠️워킹트리에 **병렬 세션 작업물 잔존**(GlitchFX/PurgeSnapshotFX/SyncGlitch·InkBlob 셰이더/주인공 캐릭터시트/렉시콘·엔딩 문서 수정/PlayerAfterimage 확장) — 그쪽 소유, 건드리지 말 것.

## 오늘 채택된 것 (전부 플레이 검증)

1. **★워크플로우 전환(유저 지시)**: 메인 세션=디렉팅만, **모든 구현=에이전트(Gameplay/Stab/artist) 위임 + `model: fable` 강제**(Opus 기본값 금지). 구현→Stab+Codex 병렬 리뷰 유지.
2. **B-008 모퉁이 랩 시공**: `CornerLab_B008`(CombatLab 동측 x60~92). 3구역(L-코너/S자 2연쇄/개활 18×22)+차폐 3단+출현 포인트 4종+발견밴드·프러스텀 마커. 좀비 7. NavMesh 아닌 직선추적이라 검증=캡슐 스윕.
3. **★시야 콘 = 상시 정보 게이트(유저 비전, Blindspot 문법)**: `VisionConeRenderer` — 레이캐스트 팬(벽=시야 그림자) → **오쏘 톱다운 마스크 → 깊이 기반 월드좌표 샘플링**(TiltShift 패스 합성). 콘∩LOS 밖 좀비 실물 숨김(`ZombieLKPSilhouette` 게이트, `RegisterImmediate` 공개 API), 시야 잃으면 회색 잔상 "기억"(2.5s), 본 적 없으면 잔상 없음. 70°→조준 35°→수렴 22°(콘 수축=수렴 게이지, 시안 오버레이 기각). 콘 방향=마우스 즉각(레이저 문법). maskDarkness 0.7.
4. **벽 처리**: 높이 컷 기각("벽 느낌 죽음") → `WallOcclusionFader`(카메라↔플레이어 + 카메라↔시선 지점 차단벽만 디더 디졸브, 발치 0.5m 솔리드). 카메라 회전은 기각 아닌 **다음 볼트 후보**(콘이 정보를 쥐므로 치트 아님 — V1=느린 추종+조준 동결).
5. **치사성**: 랩 좀비 9마리 config NULL=**무적 버그** 수정 + `ZombieConfig_General.maxHP 12→2` = 기본 바디샷 1발 킬(전 씬 영향). Signal HP 20은 미조정.
6. **E-001 판돈 볼트온**: 기존 RunLoop 스켈레톤에 배선 — 깊이 가중 드롭(A1/B2/C3, footprint x59~92), 사망 시 "메모리 N개 소실", EP C북단(74,0,43) 뱅킹 루프, 귀로 웨이브 4(페이즈 가드·즉시 게이트 편입·정면 8m 공정성). HUD "메모리: N"(렉시콘).

## ★게이트 1차 판정 + PM 진단

- PM 진단(전문은 세션 로그): **"무대 완성, 판돈 0" — 아쉬움=70% 수지 공백.** 시야 콘은 4번째 질감 시도가 아니라 정보 게임 골격(저격 연출 기각 vs 콘 채택이 증거). 덕코프=콘+익스트랙션 시장 검증, 단 심장은 경제. **Zone(B-009 잔여)을 경제보다 먼저 올리면 "이유 없이" 기각 4회차 위험.**
- 유저 1차 판정: **"아 좋은데? 괜찮아"** — 70% 가설 생존. 손볼 건 내일.

## 다음 세션 큐

1. **경제 본편 후보**: 수지 곡선·메모리 소비처(사무실), 탈출 대기 18s 공정성 판정(현재 AFK 탈출 불가 — 의도적 압박인지 유저 콜), Swept 소실 표기.
2. **카메라 회전 V1 볼트**: 평시 느린 yaw 추종(데드존 ±30°/45°/s)+조준 동결. 콘과 한 몸.
3. 미결: Signal 좀비 HP 밸런스 / 출처 미상 앰비언트 12마리 스폰 추적(ZombieSpawner GO 비활성인데 스폰됨) / 정산·사무실 "보유 strain" 표기 렉시콘 위반 잔존 / 기존 GateLab_Corner 정리 여부.

## 함정 기록 (재발 금지)

- **에디트 모드에서 `SendMessage("LateUpdate")` 금지** — LKP 게이트가 에디터에서 돌아 좀비 렌더러 80개가 꺼진 채 씬 저장된 사고. LKP/페이더는 플레이 전용 가드 추가됨.
- **퍼스펙티브 카메라로 시야 마스크 찍기 금지** — 스크린 3탭이든 높이 적층이든 경계 N겹 필연. 정답=오쏘 마스크+깊이 월드좌표 샘플링.
- RunManager.Settle의 timeScale=0이 Time.time 기반 검증 모니터 동결 — 플레이 검증은 unscaled/프레임 카운트 + `isPaused=false` 강제.
- 좀비 시야/이동 차폐 표준 레이어 = **Obstacle(8)**(총알·대시 마스크 동일). Default 벽은 전부 뚫림.
- 병렬 세션 공존 시 git: 파일 단위 선별 커밋 + 고아 index.lock(1시간 묵음) 발견 시 프로세스 확인 후 제거.
