# 2026-07-11 조작감 동기화 슬라이스 — 발 미끄러짐 · 공격 선딜 핸드오프

> 유저 진단: "뛰는 게 아니라 슬라이딩하는 느낌 + 공격에 선딜" → 나·Codex 독립 진단 수렴 → 전부 하나씩 착공.
> 레퍼: 하이포니(HIGHPONY SPACE:ERROR) 릴 프레임 실측 — 슬래시 아크 ≤2프레임 즉발, "뛴다"는 기울기·실루엣이 판다.

## 진단 확정 (실측)
- **슬라이딩**: 로코모션 재생율이 실측 속도 비종속(런 트리 고정 1.35×) — 스프린트 9/15/24 m/s에서 발이 안 빨라짐. + RunFeel 감속 13 → 3단에서 키 뗌 후 1.85초 활주. + 씬별 moveSpeed(4.31/5/7) vs walkSpeedRef 5 불일치.
- **선딜**: 코드/전이 지연 0 (ANY→Combo1 dur 0/offset 0). 원인 = 1타 클립 윈드업: 스윙 시작 0.238s(14.3f), 히트 0.269s.

## 시공 내역
### P1 — 1타 리타임 (Animation 에이전트)
- `KatanaComboRetimer.cs`: per-combo `windupSpeed` 필드 신설. Combo1만 3.5 (2·3타 1.25 유지).
- Combo1: 스윙 시작 0.238→**0.085s(5.1f)**, 히트 **0.117s**, 캔슬창 0.170s, 총길이 0.538s. **히트 이후 여운 0.42s 불변**("발동 즉각, 여운 길게").
- Combo2/3: 스윙 시작 0.125/0.117s로 이미 기준(≤0.15) 충족 → 불변.
- 노브: `Combo1WindupSpeed` 유효범위 [2.97, 4.35] — 더 빠르게는 ~4.2 (히트 0.10 바닥).
- 트레이드오프: 윈드업(3.5)>스트라이크(2.2) 역전 — 플레이어=DMC식 즉응 우선(적 텔레그래프 캐넌과 별개).

### P2 — 발속도 종속 + 딱멈춤 (오케 직접)
- `PlayerAnimatorDriver`: **LocoRate** = 실측속도 ÷ 애니커버속도. 앵커 `walkAnimSpeed 4.31`(S2_Run 1× 루트모션 실측, 06-19) / `runAnimSpeed 5.82`(×1.35). 상한 `locoRateMax 2`. 컨트롤러 `LocoRate` float 파라미터 + Base Locomotion·UB_Loco 상태 Speed Multiplier 바인딩(둘 다 같은 파라미터 = 클럭 동일, 07-04 Fix B 위상 정합 유지).
- 댐핑: speedDamp 0.1→0.035, dirDamp 0.08→0.03.
- `PlayerMotor`: **stopTime 0.15** — 입력 완전 0일 때만 감속률 max(decel, |v|/stopTime). 꺾기는 기존 decel(곡선 질주 관성 보존, 06-28 관성화 존중). 0=끔(A/B). 무빙 평타 locked 경로에도 동일 적용(Codex P1).
- **잔여 한계(구조적)**: 상한 2배로 커버 가능한 속도 ≈ 11.6 m/s — 스프린트 2단(15)·3단(24)은 잔여 슬라이드 있음(1.3×/2.1×). 초과분은 연출(P3+속도언어) 담당. 전용 스프린트 클립 도입 시 소멸.

### P3 — 달리기 기울기 (오케 직접)
- 이동 방향 수평직교축 기준 비주얼 루트 틸트. `leanMaxAngle 10°`(0=끔) / `leanFullSpeed 15` / `leanDamp 0.12`. 걷기 4.31≈2.9°, 1단 9≈6°, 2단+≈10°.
- `_facingRot` 분리 상태 신설 — 회전 보간·이동 투영·폴백 전부 순수 facing 소스로 단일화(lean 오염 원천 차단). 공격/대시 진입 시 lean 하드컷.
- 관절 포즈는 안 건드림(비주얼 루트 틸트 1회) — 애니가 진실 유지. 2차 모션·캐릭터 모션블러는 미착공(별도 조사: URP per-object MB 지원 여부 미검증).

### 씬 값 정리 (5개 랩)
| 씬 | moveSpeed | walkSpeedRef/runSpeedRef | 비고 |
|---|---|---|---|
| RunFeel_Whitebox | 4.31 (accel 16/decel 13 유지) | **4.31/5.82** (발-참) | stopTime 0.15 추가 |
| _PlayerStackTest / SlashLab_Closeup | 4.31 | **4.31/5.82** | 〃 |
| _CombatSlice_ReadAndCut / _AtomLab_OneCut | 7 | **5/8.5 (되돌림 — Stab High-1)** | 〃 |
- Stab High-1: moveSpeed 7 씬에 발-참 refs를 넣으면 평시 걷기가 blend 2.0 포화 = 항상 풀 런 게이트로 보임(신규 회귀). refs 5/8.5로 되돌려 어제 그림 보존 — **LocoRate 수학은 ref 무관하게 발을 맞춤**(blend 1.57일 때 기대 발속 5.17 = 실제 혼합과 일치).
- ⚠️A/B 옵션: 7 m/s는 물리적으로 달리기 속도 — 그 두 씬 refs를 4.31/5.82로 바꾸면 "평시=풀 런 게이트+발 정합"이 됨. 유저 판정.

## 게이트 (Stab+Codex 병렬) — 지적 처리
- **Codex P1-1** (대시가 글라이드 안 지움): 기본 경로는 오탐(StartDash L315가 지움). 단 "대시 시작 같은 프레임 후반 애니 이벤트가 글라이드 재주입" 이론 틈 → UpdateDash에서도 소거(불변식 봉인). ✅
- **Codex P1-2** (무빙 평타 중 stopTime 우회): locked 경로에 동일 적용. ✅
- **Codex P2-1** (대시 방향 폴백이 leaned forward): 오탐 — 폴백은 루트(비회전) forward, lean은 비주얼 자식 전용. 무수정.
- **Stab High-1**: 위 표 참조(두 씬 refs 되돌림). ✅
- **Stab M-2** (컨트롤러 손편집 공백): 에디터 정규형 재저장 + LocoRate 파라미터/바인딩 2개 런타임 검증 통과. ✅
- **Stab M-3/M-4** (OnEnable lean 캡처·face==0 폴백 오염): 순수 yaw 재구성 + 폴백/투영 기준 `_facingRot` 단일화. ✅
- **Stab M-5** (walkSpeedRef↔walkAnimSpeed 개념 분리 커플링): 정보성 — High-1이 실사례. 두 값은 "매핑 취향" vs "애니 진실"로 역할 분리(문서화로 처리).
- 컴파일/콘솔 에러 0. 클립 이벤트 디스크 실측 대조 일치(Stab·Codex 공통 확인).

## R2 — "움직임이 너무 각지다" 판정 대응 (07-11, 같은 세션)
유저 1차 플레이 판정 = 각짐. 진단 3원인 + 대응 (게이트 생략=저비용 노브/국소 튜닝, 커밋 전 게이트에 합산):
1. **dirDamp 과조임(자책)**: 0.03이 8방향 스냅 버킷 점프를 노출 → **0.07 복원**(속도 반응 speedDamp 0.035는 유지 — 안티 슬라이드 축).
2. **기울기 축 플립**: 좌↔우 스트레이프 전환 시 lean 축 순간 반전 = 탁 꺾임 → `_leanVec`(축×각 벡터) 이즈로 교체(0을 지나며 미러링).
3. **8방향 스냅 자체**: 과거 의도 결정(대각 3클립 혼합 뭉갬 방지)이라 유지하되 **`snap8Way` A/B 노브 + 플레이 중 G키 토글** 신설 — OFF=연속 방향 블렌드. 판정=유저.
4. 미대응(별개 축): *경로 자체*의 각짐(WASD 꺾임 물리)은 accel/decel·회전관성 영역 — 유저 확인 필요.

## R3 — "8방향으로밖에 못 움직이나? 하이포니처럼은 안 되나?" (07-11, 같은 세션)
유저 2차 판정 = 이동 궤적 자체의 8방향성. 답 = **조향(steering) 모델**: 8키 입력은 *목표 방향*일 뿐, 속도의 '방향'을 `steerTurnRate`(720°/s)로 연속 회전 + '크기'는 기존 가감속 — 궤적이 원호를 그린다(하이포니식).
- `PlayerMotor.SteerVelocity()` 신설: 걷기·스프린트 공용. 급턴=정렬도(dot)만큼 목표속도 자연 감속(카브), 180° 반전=정렬 0 → 감속→재가속(심고 돌기) 창발. 저속(steerMinSpeed 1.5 미만)·조향 0=기존 벡터 감가속 폴백(출발 즉각성 보존, A/B).
- 노브: `steerTurnRate` 720(크면 즉각=끔에 수렴, 작으면 큰 원호 — 질주 카브는 모멘텀 슬래셔 코어와 정합) / `steerMinSpeed` 1.5. 씬 5개 명시.
- 게이트 생략=[게임감 반복 루프 내 국소 이동 로직] — 커밋 전 게이트에 합산 예정. 컴파일 0에러.

## R4 — "방향 꺾임이 물처럼 안 흐른다" (07-11, 같은 세션 — 유저 3차 정정: 정지 아님, *꺾임의 흐름*)
방향 전환 흐름의 3층으로 재정리:
1. **궤적**: 조향 720→**360°/s**(90° 꺾기 = 0.25초 원호 — 720은 걷기에서 안 보임). + **정지 유예 `stopGraceTime 0.08`** — 키 롤오버(W떼고 D 사이 무입력 프레임)가 stopTime 급제동을 트리거해 *꺾을 때마다* 멈칫하던 것 제거(이것도 꺾임 흐름용).
2. **몸 스윙**: 하이브리드 걷기는 몸이 커서 고정이라 꺾여도 몸이 안 돎 = 궤적이 곡선이어도 딱딱하게 읽힘. → **RunFeel을 흐름 프리셋으로**: facingMode=FaceMovement(0), faceTurnRate 600→480(스윙 여유). F키로 복귀 비교.
3. **다리 블렌드**: RunFeel snap8Way=0(연속) 기본. G키로 복귀 비교.
- 전투 씬(CombatSlice/AtomLab 등)은 하이브리드/스냅ON 유지 — 걷기 문법 판정은 RunFeel에서 먼저.
- 판정 후 결정: 걷기 facing 기본값(FaceMovement vs 하이브리드 v2[걷기=이동, 조준행동만 커서]) + steerTurnRate 확정.

## R5 — 지각 임계 진단 + 과장 프리셋 (07-11, 같은 세션) ★유저 1차 승인
유저: "조향? 스냅? 차이 안 보임" → 진단: 전부 정상 작동이나 **지각 한계 아래로 튜닝**돼 있었음(조향 360=곡선반경 0.7m=쿼터뷰에서 점, lean 걷기 3°, 스냅은 FaceMovement에선 무의미[다리가 항상 앞걸음]). 게임감 정석대로 **과장→수렴**: RunFeel을 조향 180(반경 1.4m)·몸회전 300·lean 14°@8m/s로 과장 → **유저 "좋아 괜찮네" 승인**. H키(조향)·G키(스냅, Hybrid에서만 유의미)·F키(facing) 하니스 완비.
- ★함정 2건 기록: ①씬 YAML 디스크 편집 후 **에디터 씬 리로드 필수**(에디터 메모리가 이김 — "차이 없다" 오판 원인) ②유저 플레이 중이면 OpenScene 불가 → SerializedObject 라이브 주입으로 즉시 반영.
- 미확정: 과장 값에서 어디까지 되돌릴지(현행 승인치 기준 미세 조정), 전투 씬 이식 여부.

## R6(완료) — 제자리 회전 발 셔플 (Animation 에이전트)
유저: "마우스 돌릴 때 발은 가만히 몸만 도는 게 어색" → turn-in-place.
- **턴클립 기각 사유**: Frank_Sword2_17_Turn_45_90_180은 열어보니 **검+방패 스탠스**(Shield_Bone 존재) — 카타나 Stance1과 상체 상극(블렌드 시 방패자세 모프). 카타나 팩 네이티브 턴클립 없음(전수확인).
- **채택 = TurnShuffle 중첩 트리**: Locomotion Speed=0 노드를 1D 트리(param `TurnRate`)로 확장 — −1=좌 스트레이프/0=idle(기존 클립 그대로)/+1=우 스트레이프. 기존 walk와 동일 스탠스·loop·in-place 클립 재활용 = 모프 위험 0, 회귀 0. **회전 각도·속도는 계속 코드(_facingRot) 소유** — 드라이버는 yaw 각속도 관측만 해 TurnRate 공급.
- 노브(드라이버): `turnShuffleThreshold` 55°/s(데드존) · `turnRateRef` 200 · `turnShuffleMax` 0.55 · `turnShuffleDamp` 0.09 · `turnShuffleInvert`(좌우 A/B).
- 판정 포인트: ①0.55 강도가 "은은한 무게이동"인지 "달리는 다리"인지(후자면 max↓ — 유일한 실질 리스크) ②좌우 매핑 자연스러움(invert) ③부감 15m에서 임계 적정. ⚠️테스트는 F키로 FaceMouse/Hybrid에서(FaceMovement 정지 시 마우스가 몸을 안 돌림).

## R7 — 대시 물리감 (07-11, 같은 세션) — 유저: "슉! 나가고 딱! 끊겨 어색, 물리 효과 필요"
구 대시 = 등속 30m/s→한 프레임 0(딱 멈춤 의도 설계). 두 레버로 물리화:
1. **이즈아웃 프로파일** `dashEasePower 1`(0=기존 등속 A/B) — 위치곡선 x=D×(1−(1−t)^(1+e)), 총 거리 보존(밸런스 불변)·프레임레이트 무관. 시작 버스트 2배(더 슉!)→끝 감속.
2. **출구 모멘텀** `dashExitMomentum01 0.6` — 종료 프레임에 moveSpeed×0.6을 로코모션 승계. 키 홀드=조향이 받아 달리기 연속, 무입력=stopTime 스키드(0.08 유예+0.15 정지), **벽 충돌 종료=0(꽝)**. R3~R4 시스템에 자연 합류.
- 대시캔슬 공격(발도 정지 캐넌)은 locked 브랜치가 즉시 0으로 잡아 불변. 씬 5개 명시+RunFeel 리로드 검증 완료. 컴파일 0에러.

## R8 — 대시 연출 2문법 A/B (07-11, 같은 세션) — 유저: "회피가 맛대가리 없음, 장르는 어떻게 하나" → "둘 다 보여줘"
장르 해부(3비트: 킥오프/이동/착지 — 맛은 앞뒤 비트) 후 두 킷 시공. PlayerAfterimage 확장(기존 잔상=이동 비트 위에 얹음, 절차 생성·풀링 이디엄 유지):
- **Glitch(위상 스텝 — RUINER 북극성·마젠타=신호붕괴 캐넌)**: 킥오프=마젠타 붕괴 샤드 팝, 착지=시안 재조립(안으로 모임), i-frame 꼬리 동안 성긴 고스트(위상 상태 가독).
- **Physical(데스도어 — 먼지·무게)**: 킥오프=뒤로 차는 먼지, 착지=관성 방향 먼지+스쿼시(DOTween, 스케일만 — 포즈 불가침).
- 공통: 대시 방향 카메라 마이크로 킥(PlayerCameraFollow.AddKick 0.08m), whoosh/스커프 SFX 슬롯(2D 플레이스홀더, Sound 에이전트 선곡 중).
- **J키 순환** Off/Glitch/Physical + HUD 2번째 줄. 노브: 샤드 수·색(HDR)·먼지 수·스쿼시 배율/시간·킥·볼륨.
- 게이트=[게임감/주스] — 커밋 전 일괄.
- **픽스**: 내장 Default-Particle 런타임 로드 불가(유저 콘솔 에러 보고) → 소프트 원 텍스처 절차 생성으로 교체.
- **SFX 1차 배선 완료(RunFeel 씬 저장됨)**: whoosh=`Whoosh Short Driven 01`(0.10) · scuff=`Footsteps_DirtyGround_Jump_Land_02`(0.09, 발소리 팩과 응집). ★Sound 에이전트 발견=글리치용 디지털/신스 whoosh 부재(라이브러리=순수 물리 폴리) — 선택지: Metal 프록시 / 절차 신스 합성(정체성이라 정당, 오케 추천) / Feel 데모 NVTeleport(⚠️라이선스 미확인). 변주 배열(스팸 내성)은 판정 후. 음색 판정=유저 귀.

## R9 — 대시 속도감 손맛 (07-11) — 유저: "화려함 말고 속도감·확! 피했다·슬라이딩"
진단: 0.1s(6f) 대시 = 이동 과정이 지각 불가 → 순간이동으로 읽힘(속도감 0). VFX가 아니라 모션·카메라 레버 4개:
1. **이동창 연장** dashDuration 0.1→**0.15**(10f — 이동이 보임), dashDistance 3→3.2 (평균 21m/s).
2. **출구 슬라이드 절대값화** — dashExitMomentum01(moveSpeed 비율 ≈2.6m/s, 체감 0) 폐기 → **`dashExitSpeed` 8m/s** 인계 → stopTime 감쇠로 ~1.2m 관성 슬라이드("확 나가고—미끄러지며—정착").
3. **카메라 킥 방향 반전** — 진행 방향 밀기(캐릭터 화면 고정=속도감 죽임)를 **반대로 당김**(캐릭터가 프레임 앞으로 돌출) + 0.08→0.22.
4. **FOV 서지 펀치** 1.5°(기존 줌펀치 채널 재사용, 밀도 억제 포함).
- 씬 5개 갱신(RunFeel=에디터 재직렬화라 MCP로, 나머지 4=YAML). 컴파일·저장 완료.

## R10 — Codex 구조 진단 + 1단계 시공 (07-11) — 유저: "자연스러움·속도감·agency 없음, Codex랑 얘기하자" → 1단계 승인
**Codex xhigh 구조 진단(원문=세션 로그)**: ①입력 체인이 "무기 busy→모터 잠금→애니가 결과 관측" — 입력이 '잠김'으로 먼저 체감 ②애니메이터가 위치 델타 관측자 — 발이 "누른 방향"이 아니라 "밀려난 결과"를 따름(agency 손실 핵심) ③애니 어휘 부족(같은 런 루프 재사용, start/stop/pivot 없음 — 수치 보정으로 대체) ④대시: 자유 벡터 이동 vs 4방향 스텝 시각 분열 ⑤카메라=FX 후처리(액션 문법 밖). **흐름vs agency는 본질 트레이드오프 아님** — 스무딩이 1차 체인에 있고 '의도 표현' 계층이 없어서 생긴 것. P0=의도/물리/애니/카메라 채널 분리+액션 스테이트, 콤보 상체 패치 재검토(⚠️07-04 상하체 분리 번복 판돈), P1=애니 어휘·카메라 편입·노브 정리.
**나와의 수렴**: 애니 어휘 천장(완전 일치)·1차 체인 스무딩(방향 일치). **발산**: 나=스무딩 재배치(속효) vs Codex=의도 채널+구조 재설계(근본). **유저 선택 = 1단계(속효+국소)**:
- ⓑ **의도 채널 최소 도입**: PlayerBrain→driver.Tick(input.move) — 발/facing/기울기 방향이 위치 결과가 아니라 **입력 의도를 즉시** 따름(크기 Speed는 실측 유지). 외력 이동(넉백)만 델타 폴백.
- ⓒ **대시 시각-궤적 정합** `dashFaceMotion`(기본 ON): 대시 순간 몸=실제 대시 방향(자유 벡터)+전진 스텝 — "몸은 대각선, 발은 정방향" 분열 해소(하데스식 커밋). OFF=구 4방향 스냅.
- ⓐ **이동 즉각화**: steerTurnRate 전 씬 0(H키로 360 재비교), RunFeel accel 16→50/decel 13→40. 부드러움은 시각(몸 회전 300·lean·dirDamp)만.
**2단계 후보(1단계 판정 후)**: 액션 스테이트 재설계(Codex P0 전체) · 애니 어휘 소싱(start/stop/pivot — Animation 에이전트) · 콤보 전신 승격 판정 · 카메라 액션 편입.

## R11 — 대시 애니 Evade 교체 + 잔상 붉은사막화 (07-11) — 유저 지시
- **애니(완료)**: DashDir 트리 Step→Evade_F/B/L/R 교체(guid 스왑만) + Dash 상태 m_Speed 1→**2.2**(단일 리타임 노브 — 클립 0.8s 원속이면 0.15s 창에 윈드업만 보임. 2.2×로 이탈 정점이 0.076s, 도약 80%가 0.138s에 들어옴). 회수 구간은 반응성 우선으로 컷. Bake 설정 무변(전부 OFF가 정답 — 루트모션 폐기 경로. ★06-20 "BakeRot ON" 노트는 루트모션 대시 시대 유물로 정정). 속도 다이얼 1.8(자연)~2.6(스냅). 발-슬라이드는 구조 내재(클립 3.28m 암시 vs 코드 이동) — 화이트박스 수용. Step FBX는 미참조로 잔존(삭제 안 함).
- **잔상**: 대시 트레일·스트릭·위상꼬리 제거 → **출발점 실루엣 1장 0.45s 페이드**(붉은사막 문법). 고스트 개별 수명(g.life) 도입, 실루엣은 대시 첫 프레임 변위(-_lastDelta)만큼 되돌려 원점 정렬. 노브: trailDuringDash(구 방식 A/B)·startSilhouetteCount/Lifetime. 스킬 런지 EmitBurst 경로는 불변.

## 세션 종료 상태 (07-12 마감)
**✅유저 승인**: R5 흐름 프리셋(과장값 — "좋아 괜찮네") · R10 1단계(의도 채널·대시 정합·이동 즉각화) 반영 상태에서 R11 대시 패키지(Evade+실루엣+슬라이드+카메라) **"괜찮네" 최종 승인** → 문서 정리 후 종료.
**미판정 소항목(다음 세션에서 플레이 중 자연 판정)**: ①선딜 스미어 뭉갬 여부(`Combo1WindupSpeed` 2.97~4.35) ②기울기 14° 과함 여부 ③고티어(15/24) 잔여 발슬라이드 ④CombatSlice/AtomLab 걸음새(5/8.5 유지 vs 발-참) ⑤comboMoveSpeedMult 씬 불일치 ⑥제자리 셔플 강도/좌우(`turnShuffleMax`/invert) ⑦Glitch whoosh 디지털 음색(절차 합성 추천, NVTeleport은 라이선스 미확인) ⑧대시 연출 스타일 확정(J키 — Glitch/Physical/Off 중).

## 변경 파일 (전부 미커밋)
`Player/PlayerMotor.cs`(조향·stopTime·유예·대시 이즈아웃/출구·DashDir) · `Player/PlayerAnimatorDriver.cs`(LocoRate·lean·의도채널·TurnShuffle 공급·dashFaceMotion·하니스 F/G/H) · `Player/PlayerBrain.cs`(Tick(input.move)) · `PlayerAfterimage.cs`(대시 비트 2킷·시작 실루엣·SFX·카메라 서지·J키) · `KatanaMelee.controller`(LocoRate·TurnRate/TurnShuffle·DashDir=Evade·Dash speed 2.2) · `Editor/KatanaComboRetimer.cs` · `S1_Combo01_01/02/03_Retimed.anim` · 랩 씬 5개(직렬화 값+SFX 배선).
- ⚠️RunFeel 씬의 whoosh/scuff 클립 참조는 **로컬 전용 팩**(Hack and Slash=gitignore) — 다른 머신에선 missing ref(기존 정책과 동일, 07-05 결정).
- 클립 원본 백업 = 세션 스크래치패드 `anim_backup/`.

## RESUME (다음 세션 순서) — ⚠️07-13 승계: 최신 RESUME = [[2026-07-13-dash-rootmotion-handoff]] (R12~R14c 대시 루트모션 전환 반영. 아래는 07-12 시점 기록)
1. **커밋 전 일괄 게이트(필수 — 재량 없음)**: R2~R11 누적분 Stab+Codex 병렬. 오늘 R2 이후 라운드는 "게임감 루프 내 국소 튜닝" 클래스로 게이트를 커밋 전 합산 선언해뒀다 — 컨트롤러/씬/직렬화 에셋 대량 변경이라 객관 트리거.
2. 게이트 통과 → **커밋** (유저 확인 후).
3. **애니 어휘 소싱** — 나+Codex 완전 수렴한 다음 최우선 과제: 출발/정지/피벗/브레이크 전용 클립(Frank 팩 전수 재조사→부족분 MoCap Online 등 소싱, Animation 에이전트). "다른 게임 같은 자연스러움"의 남은 천장.
4. 미판정 소항목 ①~⑧ 플레이 판정 → 하니스 키(F/G/H/J·OnGUI) 제거 + 승인값 굳히기 + 전투 씬 이식.
5. (1단계로 부족 판명 시) 2단계 = Codex 액션 스테이트 재설계(의도/물리/애니/카메라 채널 분리·콤보 전신 승격 판정·카메라 액션 편입).
6. 백로그: Glitch whoosh 절차 합성 · SFX 변주 배열(스팸 내성) · 2차 모션/캐릭터 모션블러 조사(URP 제약 미검증) · 스프린트 전용 클립.
