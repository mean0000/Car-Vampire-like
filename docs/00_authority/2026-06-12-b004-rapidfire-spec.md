# B-004 — 연사 타격감 "트랜지언트의 행렬" 스펙

> **작성**: 2026-06-12 · **상태**: v1.1 — v1.0 동결 후 **시공 완료**(같은 날, 세션 2 — 유저 지시로 E-002 게이트 전 당김). 시공 현황·노브 맵·게이트 체크리스트 = §8 · (구)유저 판정 2건: ①범위=코어 4채널만 — 탄피 카펫·이동 제약 제외 ②명중 thud 포함
> **재료**: 코드 현황 조사(Explore) + 부감 연사 레퍼런스 리서치(Vlambeer 30트릭·Nuclear Throne·The Ascent·SYNTHETIK) — 본 문서가 둘의 종합 권위.

## 1. 가설 재정의 (원문이 낡았다)

- **백로그 원문**: "연사의 빈자리(카메라 충격 0)는 히트스탑·머즐 리듬·사운드로 채울 수 있다"
- **코드 조사 발견**: 빈자리의 절반은 이미 차 있다 — 개체 단위 히트스탑(hitStopNormal 0.04s, 전역 timeScale 아님), 경직 사다리(flinch 0.15→3타 stagger 0.4→6타 knockdown 1.5), 머즐 플래시/라이트, 서스테인 카메라 오프셋(0.05m), 넉백, 흰 점멸, 스쿼시. **주스 교과서 ②와의 모순은 없었다** — 이미 올바른(개체 단위) 방식으로 구현돼 있음.
- **진짜 빈자리**: 채널이 아니라 **리듬**. 현재 연사는 매 발이 균질하다(같은 플래시·같은 소리·같은 잔광) → 루프로 뭉개져 무게가 죽는다.

> **개정 가설 (게이트 질문)**: "연사가 균질한 루프가 아니라 **세어지는 트랜지언트의 행렬**(시각 박자 + 세계의 반응 + 소리 행렬)이 되면, 카메라 충격 없이도 대량 정화 동사가 노래한다."
> 게이트: 연사로 호드를 갈 때 발 하나하나가 세어지는가? 카메라 충격이 그립지 않은가?

## 2. 리서치 3대 원칙 (종합 판정)

1. **카메라가 못 떨면 세계가 떨어라** — 충격 예산을 카메라에서 월드로 전액 이전: 실루엣 변위(넉백 누적·경직·시체)와 영구 흔적(탄흔·탄피·시체)이 부감의 화폐. (기존 판정 "실루엣 변위+영구 흔적"의 연사 확장)
2. **연사는 루프가 아니라 트랜지언트의 행렬** — 균질 반복(매 발 동일 플래시·트레이서·사운드)이 무게를 죽인다. 발당 개별성 + 주기적 박자(N발당 1 강조)로 "세어지는 연사"를 만든다.
3. **반동은 카메라가 아니라 몸과 선택이 먹는다** — 셀프 넉백·이동 제약·조준선 거칠어짐 = 연사를 비용 있는 전술 선택으로. (B-009 "원샷↔이동둔화 짝"과 같은 문법)

## 3. 볼트 패키지 — 코어 4채널 (전부 기존 시스템 확장, 신규 시스템 0)

| # | 채널 | 구현 | 시작값(노브) | 근거 파일 |
|---|---|---|---|---|
| C1 | **트레이서 박자 4:1** | N발당 1발만 굵은 비행 꼬리+가산 글로우, 나머지는 얇은 잔광만 | tracerCadence=4, 강조발 폭 0.07/일반 0.015(기존 값 재배치) | PlayerCombat.cs 616-644 (기존 트레이서 변조) |
| C2 | **머즐 변조 + 광원 스트로브** | 플래시 크기·라이트 강도 발당 지터, 강조발(C1과 동기) ×1.3 — 부감에선 플래시 본체보다 바닥에 깜빡이는 광원이 본체(The Ascent 증거) | flashSizeJitter ±20%, lightIntensityJitter ±25%, 강조발 배수 1.3 | PlayerCombat.cs 86-106, 1140-1175 |
| C3 | **사운드 행렬** | ①발당 트랜지언트 + 피치 지터(SfxOneShot 기존 파라미터 활용) ②마지막 발 테일(연사 종료 잔향) ③**명중 thud 별도 채널**(발사음과 분리 — CoD 히트마커 문법, "사운드=정보채널" 판정 정합) | pitchJitter ±5%, thud 볼륨 0.06(보수적 — 못 듣는 원칙), tailVolume 0.08 | GunSfx.cs, SfxOneShot(pitchJitter 파라미터 기존재) |
| C4 | **넉백 누적 제압 + 그레이즈 노브화** | 연사 탄막이 호드 전진 벡터를 꺾는 게 보이게 — 기존 weakKnockback 튜닝 + grazeFlinchChance 하드코딩(0.35) SO 노브화. 엘리트(Signal)는 넉백 감쇠로 무게 차등(Brotato 문법) | weakKnockback 4.4(기존)에서 게이트 연장으로 조정, grazeFlinchChance SO 이동 | ~~PlayerCombat.cs 1113~~ ★§8 정정 — 시공 시점에 이미 `nearMissFlinchChance` SO 노브로 이전돼 있었음(스펙 정보 낡음) |

**원칙 준수**: 노브+롤백(전 채널 마스터 토글), 1가설(트랜지언트의 행렬) 1게이트. 신규 시스템 없음 — 전부 변조.

## 4. 옵션 채널 (판정 대상 — 포함 여부 유저 콜)

- **O1 탄피 카펫**: 연사한 자리에 쌓이는 놋쇠 데칼(상한 풀) — "여기서 갈겼다"는 영구 증거. 난이도 낮음, 단 채널 수 +1.
- **O2 연사 중 이동 제약/셀프 푸시**: 연사 지속 시 이속 저하 — 전술 비용 축. 핸들링이 바뀌는 결정이라 단독 게이트 가치가 있음(이번 볼트에 섞으면 가설 오염 위험).

## 5. 기각/이연 채널

- 글로벌 히트스탑 — 기각 유지(레퍼런스도 만장일치로 "파티클·카메라는 흐르게").
- 매 발 스크린쉐이크/킥 — 킥 예약제 판정 위반(Gungeon도 멀미 슬라이더 논쟁).
- 조준선 확산 원뿔 — 수렴 시스템(시안)과 정면충돌. 변형(연사 시 풀라인 거칠어짐)만 후보로 보관.
- 카메라 정적 변위 — 서스테인 오프셋(0.05m)이 이미 존재. 이번 볼트에서 체감 확인만.
- 시체 발사/관통 연쇄 — 사망 연출은 FX 세션 영역(ZombieDeathFX·StrainHarvestFX)과 얽힘 + 물리 드랍 동결 직후라 이연.

## 6. 파일권·충돌 플래그 (착공 전 확인)

- **ZombieController.cs = FX 세션이 워킹트리 수정 중** — C4(넉백·flinch 튜닝)가 이 파일을 만짐. **착공 전 FX 세션 커밋 여부 확인 필수**, 미커밋이면 C4는 CombatFeelConfig(SO) 쪽 노브만 먼저.
- 카메라(PlayerCameraRig)·콘(VisionConeRenderer)은 이번 볼트 무수정(서스테인 체감 확인만).
- 사운드(C3) 구현은 **Sound 에이전트**(2026-06-12 신설, 다음 세션부터 로드) 첫 실전 — 못 듣는 원칙대로 노브 보고+유저 귀 판정.

## 7. 검증 계획

- 시각(C1·C2·C4): MCP 캡처(플레이 중 ScreenCapture — 콘 합성 함정 메모리 준수) + 게이트 플레이.
- 사운드(C3): Claude 무검증 채널 — 시스템(라우팅·발화 타이밍)만 로그 검증, 음색은 유저 묘사 루프.
- 게이트 절차: 대량 좀비(랩 또는 스포너 임시 on) 상대로 연사 30초 → "발이 세어지는가" 판정.

## 8. 시공 현황 (2026-06-12 세션 2 — v1.1 추기)

> 시공·리뷰(Stab+Codex 병렬)·연사 데모 캡처(`Temp/b004_demo_1~5.png`) 완료. **미커밋** — [[2026-06-12-threat-pass-session-briefing]] §1 규칙대로 게이트 판정 후 후방 힌트 잔무와 일괄 커밋.

### 변경 파일 (4)

`PlayerCombat.cs`(채널 본체) · `Data/CombatFeelConfig.cs`(노브 전부) · `MeleeSfx.cs`(ThudClip 공개 접근자 1줄) · `GunFlashLight.cs`(Trigger(float) 오버로드)

### 노브 맵 — 값이 실제로 사는 곳 (노브 세션용)

**전부 `CombatFeelConfig` SO. ⚠️신규 필드라 기존 .asset YAML에 아직 없음 — 진실 = C# 이니셜라이저. 인스펙터에서 한 번 저장하면 그때부터 .asset이 이김(씬 덮어쓰기 함정의 SO판).**

| 노브 | 필드 | 시작값 | 효과 |
|---|---|---|---|
| 마스터 토글 | `transientMatrixEnabled` | true | C1~C3 일괄 롤백(완전 복원 — Stab 검증) |
| C1 박자 | `tracerCadence` | 4 | N발당 1 강조발 |
| C1 강조 폭 | `tracerEmphasisWidth` | 0.07 | 강조발 웜 가산 글로우 |
| C1 일반 폭 | `tracerNormalWidth` | 0.015 | 일반발 흰 잔광(=기존 하드코드와 동일값) |
| C2 플래시 지터 | `flashSizeJitter` | 0.2 | 발당 ±20% |
| C2 광원 지터 | `lightIntensityJitter` | 0.25 | 발당 ±25% — 부감 본체는 바닥 광원 풀 |
| C2 강조 배수 | `emphasisMuzzleMult` | 1.3 | C1과 같은 발에 동기 |
| C3 피치 지터 | `shotPitchJitter` | 0.05 | 발당 ±5% |
| C3 테일 볼륨 | `tailVolume` | 0.08 | 연사(≥3발) 종료 시 발사음 피치 0.5 잔향 |
| C3 thud 볼륨 | `thudVolume` | 0.06 | 풀히트만, MeleeSfx 절차 "퍽" 재사용 |

### 리뷰 처리 기록

- **반영**: alt-fire(패닝·차지)가 박자 카운터·강조·피치지터·테일 오염 → `FireShot(primary)` 격리 (Codex). 개머리판은 원래 비경유.
- **오탐 기각**: "토글 off 시 트레이서 폭 미복원"(Codex) — Stab 전문 검증으로 반박(0.015=기존값 동일).
- **의도 유지**: 관통 다중 thud = 히트마커 문법(맞춘 수만큼) + SfxOneShot 동일클립 3개 클램프 존재. 귀 게이트 관찰 항목.
- 휴면 시한폭탄 기록: MeleeSfx 정적 클립 `??=`는 도메인 리로드 off 환경에서 2회차 무음(GunSfx식 가드 부재 — 현재 리로드 ON이라 휴면).

### 디버그 훅 (에디터 자동화 전용)

`PlayerCombat.DebugFireHeld`(static bool, Awake 리셋) · `DebugEquip(WeaponLoadout)`. ⚠️근접 시작 세션에선 원거리 풀/오디오가 null이라 무음·무탄 — 원거리 세션에서만 사용.

### 게이트 판정 기록 (2026-06-12 — 4차 재미 진단 플레이가 곧 판정)

**본게이트 "발이 세어지는가" → 아니다.** 유저가 B-004 포함 빌드로 수 회 플레이, 증언: "총을 쏘는 것 자체에서 쫄깃함이 없다"([[2026-06-12-threat-pass-session-briefing]] §0). 단 **롤백 아님** — 위협 테제(쫄깃 부재의 뿌리=좀비 위협 0)에 따라 B-004는 기층으로 유지(마스터 토글 보유), **재판정은 위협 패스 합류 후 노브 세션에서.** 트랜지언트 행렬 단독으로는 쫄깃함을 만들지 못한다는 것이 이번 패스의 실측 — "총기 채널만으로 무게 만들기"는 게임감 3연속 실패 목록에 4번째로 합류.

노브 판정 5건 (→ 노브 세션으로 이월):
1. 강조발(0.07)이 수렴샷(0.035)보다 굵은 위계 역전 — 루틴 박자가 "의도된 한 발"을 누르는가
2. 느린 무기 테일 지연(샷건 0.95s) — 잔향이 아니라 "어디서 난 소리?"인가
3. 샷건 강조 블래스트 = 펠릿 8줄 전부 굵음 — 시각 질량 과다인가
4. 관통 다중 thud — 정보인가 소음인가
5. 박자 위상 비정렬 — 연사 첫 발이 강조발로 시작 안 하는 위화감

### 이연

엘리트(Signal) 넉백 감쇠 차등(Brotato 문법) — ZombieController 파일권이 풀린 뒤(일괄 커밋 후), 세션 3의 좀비 위협 본체와 합류가 자연스러움.
