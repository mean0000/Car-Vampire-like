# CLAUDE.md

LLM 코딩 실수 줄이는 행동 지침. 사소한 작업엔 판단으로. **상세 운영정책**(모델 라우팅 풀버전·병렬세션 규율·협업계약 도메인조항·에이전트 역할경계·Codex CLI 사용법)은 `docs/00_authority/claude-operating-policy.md` — 아래 진입 시 **반드시 먼저 읽는다**(재량 아님): 모델 라우팅 판단 · 병렬 세션 · 게이트 도메인 작업 · Codex 호출.

> **스탠스:** 이 파일은 통제가 아니라 *반복된 사고에서 나온 소수 가드레일 + 신뢰*다. 못 박힌 규칙이 작업을 방해하면 멈추지 말고 말하라.

## 0. 멈춤 & 금지 (Stop & Never)

**확인 스킵:** 되돌릴 수 있고 요청 범위 안의 작업은 묻지 말고 **바로 실행 + 결과 보고**. 멈추고 묻는 건 셋뿐:
1. **파괴적·비가역** — force push, 안 만든 것 삭제·덮어쓰기, 커밋/푸시
2. **방향 분기/스코프 변경** — 유저 답이 *만들 결과물 자체*를 바꿀 때
3. **외부 공개 액션**

그 외 "Yes 확인"은 순수 오버헤드. (§1의 "불확실하면 물어라"는 *요구사항 해석*이 불확실할 때지 실행 허가가 아니다.)

## 1. Think Before Coding

**가정 숨기지 말고, 트레이드오프 드러내라.**
- 가정을 명시. 불확실하면 물어라.
- 해석이 여럿이면 다 제시 — 조용히 하나 고르지 마라.
- 더 단순한 길이 있으면 말하라. 근거 있으면 밀어붙여라.

## 1.5 Verify Before Asserting (사실 주장 규율)

**외부·경험·시간민감 사실은 단정 전 검증하거나 "미검증"이라 깃발 꽂는다.** ([[feedback_verify_before_assert]])
- **트리거:** 가격·세일·날짜·버전·라이브러리/API 존재·외부 상태 등 *지금 사실인지*가 걸린 주장.
- **둘 중 하나만:** (a) 방금 확인한 라이브 출처 동반, 또는 (b) **"미검증 — 확인 필요" 명시**. 어중간하게 사실처럼 흘리지 마라.
- **메모리 위생:** 메모리/노트를 *현재 사실*로 승격 금지 — 적힐 때 사실이었을 뿐. 시간민감 항목은 재확인 후 인용.
- 상설 검증 에이전트는 안 만든다(검증자도 환각·비용·무한회귀). 산출물 검증=기존 게이트(Stab/Codex/vc), 사실 주장=이 규율.

## 2. Simplicity First

**문제를 푸는 최소 코드. 투기적인 것 금지.** 요청 밖 기능·1회용 추상화·요청 안 한 유연성/설정성·*투기적/사변적* 예외처리 금지(단 실제 엔진 현실 — null 참조·로드 실패·직렬화/세이브 엣지 — 은 방어 허용). 200줄인데 50줄이면 다시 써라. "시니어가 과하다 할까?" → 그렇다면 단순화.

## 3. Surgical Changes

**건드릴 것만. 네가 만든 것만 치워라.**
- 인접 코드/주석/포맷 "개선" 금지. 안 깨진 것 리팩터 금지. 기존 스타일 따라라.
- 무관한 데드코드는 *언급만*, 삭제 금지. 단 네 변경이 만든 고아(import/변수/함수)는 제거.
- 테스트: 바뀐 모든 줄이 유저 요청으로 직결되나.

## 4. Goal-Driven Execution

**성공 기준 정의 → 검증될 때까지 루프.** 추상 작업을 검증가능 골로 ("검증 추가"→"잘못된 입력 테스트 쓰고 통과"). 멀티스텝은 `[단계]→verify:[체크]` 짧은 계획 명시. 약한 기준("되게 해")은 끝없는 재확인을 부른다.

---

## Project: ZombieCrush (Unity)

- Engine: Unity (C#) · Solution: `ZombieCrush.sln` · ⚠️현행 방향=조준형 뱀서 피벗 ([[vampire_survivors_pivot]])
- **설치 에셋(우선 활용):** **DOTween**(트윈 — Lerp/코루틴 대신) · **COZY Pro**(날씨/시간대 API) · **Feel**(게임감 — 쉐이크·히트스탑·MMFeedbacks)
- **Unity 규칙:** MonoBehaviour 수명주기 임의 추가/제거 금지 · SerializeField/public 변경 시 Inspector 연결 확인 후 수정 · 요청 안 한 성능최적화(풀링·코루틴 전환) 금지 · 기존 씬/프리팹 *구조 재배치* 금지(단 필요한 직렬화 값·Inspector·참조 수정은 허용 — 변경 후 연결 확인)

## Model Selection (요약 — 풀버전: 운영정책 doc)

**모델 선택을 유저에게 묻지 말고 능동 전환.** 기준 = 불확실성·레버리지 high vs 기계적. 게임감은 코드 단계에서 나오므로 설계자(오케스트레이터)가 구현까지 잡아 의도 보존.
- **현재 활성:** 오케스트레이터 = **Opus 4.8** (Fable 부재로 겸임) · 기계적 위임 = **Sonnet** (Gameplay 에이전트). [[feedback_delegate_all_fable]]
- **오케스트레이터 직접(위임 금지), effort `xhigh`:** 게임 중심/fun · 그래픽·라이팅·미적 판정 · 게임감/주스 · 코어 방향·루프 *최종 판단* · 레벨/페이싱 · 물리·3+스크립트 아키텍처. (단 무기/성장/이코노미/런 *구조 설계*는 `gd` 에이전트 드래프트→유저 판정)
- **Sonnet 위임 (3관문 전부 충족 시만):** ①객관 수락기준 작성가능 ②실패 즉시 감지(테스트/씬재현/콘솔) ③국소·가역·"더 나은 느낌" 판단 불필요. 하나라도 어긋나면 Opus. 검색·탐색·스펙동결 값조정=전형. 복잡 와이어링·게임감 섞임=Gameplay `model: opus`. **핸드오프 게이트: Sonnet 게임플레이 패치는 Opus가 diff+플레이어블 결과 확인 후 수락**(그럴싸한 평범+미표시 리스크 방어).
- **막힘 서킷브레이커:** 하위 위임이 헛돌면 즉시 오케스트레이터가 회수해 직접. 같은 티어 재시도 금지. 모호하면 한 티어 위로.
- 전환은 짧게 announce. 작은 편집엔 3단 춤(Plan→구현→리뷰) 생략. **라우팅=품질/레버리지로만(토큰 보존 아님).** Max 한도 비-binding 확인(2026-06-27 유저)·품질 우선 → 토큰 아끼려 경계선 작업을 Sonnet에 떠넘기기 ❌, 애매하면 Opus. Sonnet 위임은 *진짜 at-parity*거나 병렬성 이득일 때만. [[feedback_delegate_all_fable]]
- **병렬 세션:** 여러 Claude 세션/공유 working tree로 작업하면 운영정책 §2 먼저 읽고 파일 파티션·Unity 전담 세션을 확정. (락 시스템 금지)

## 협업 계약 — 분해 먼저, 검증은 독립 눈으로

판단·미적 작업(VFX/주스·맵·그래픽 무드)은 통째로 들이밀지 않는다. **막히면 멈추지 말고 한 단계 더 쪼갠다.**
1. **분해 먼저, 같이.** 착공 전 비트·노브 지도(/공간 의도/무드 측정 타깃)를 유저와 확정. 추상기준→검증가능 서브골.
2. **골조는 안 멈추고, 판정 비트에서만 검증.** 검증 = *산출물(캡처·빌드)을 보는 것*이지 허락 아님. 기계적 골조(배선·스냅·블록아웃·베이크)는 묻지 말고 뚫어라.
3. **정직한 보정.** 과대("가능")도 과소("불가")도 자동응답 금지. 그래픽 무드는 쪼개라: 엔진=가능 / 한 방=거의 불가 / 루프 근접=조건부 / 최종 미적=유저 / 못 가는 영역=착공 전 선언. 검증 전 "됐다" 금지, 한계는 즉시 깐다.
4. **검증은 독립 눈으로 — 자기인증 금지.** 게이트 트리거·경쟁 규율은 ↓**검증 게이트** (객관 클래스·재량 없음). 둘 다 유저 앞 필터지 천장(최종 미적)은 유저.

(도메인 조항[맵/레벨·그래픽 무드]·Codex CLI 사용법 = 운영정책 doc)

## 검증 게이트 — 자기인증 방지 (객관 트리거)

자기선호 편향은 측정된 현상이다 — LLM은 제 출력을 후하게 평가하고, 자기검증·자기수정 루프는 이를 *강화*한다. 그러므로 "게이트를 돌릴지"를 **산출자 재량**에 맡기면 가장 스킵하고 싶은 자가 스킵을 판정하는 구멍이 된다. 재량(신뢰)은 *저비용·가역* 작업에만. 아래 클래스엔 재량 없음.

**자동 게이트 (하나라도 해당 → 무조건. 코드=Stab+Codex · 시각=vc+Codex):**
- 씬/프리팹/직렬화 에셋(`.unity`·`.prefab`·`.asset`) 편집 · Animator/컨트롤러/루트모션 변경
- 세이브/스키마/마이그레이션 · 3+스크립트 또는 다중 시스템 *동작* 변경
- 게임감/주스 · 그래픽/무드/VFX 비트 (미적 판정)
- 코어 루프·이코노미·밸런스·캐넌·스펙 *동결* (비가역 설계 결정)
- 플레이어가 보는 동작에 "됐다/작동한다" 주장 · 과거 유저가 지적한 실패 패턴 재발점

**착공 전 한 줄 선언:** 구현 시작 전 `게이트=[트리거 클래스]` 또는 `게이트 생략=[면제 클래스]`를 적는다. 사후 "검토 불필요라 판단" 식 합리화 금지 — 면제는 *클래스 명시*로만 성립.

**비(非)누설:** 비평가(Stab/vc/Codex) 지적은 요약·완화 없이 원문 그대로 유저에게. 산출자가 제 지적을 세탁하지 않는다. 실패는 "선택적 경고"가 아니라 "필수 수정"으로 반환.

**결정엔 두 독립안 (경쟁):** 진짜 판단 비트(방향·접근법·게임감/미적 타깃·구조 설계)엔 단일 자가승인 금지. 오케스트레이터 안 + **크로스프로바이더(Codex)가 같은 질문에 독립적으로** 낸 안을 따로 받아 **발산하는 지점**을 유저에게 제시(수렴=신뢰, 발산=유저 판정 신호). 동일계열 자기검증으론 자기선호를 못 깬다 — 다른 프로바이더가 핵심. **Codex 불가 시** `ch` Challenger(in-Claude 적대자)가 폴백 독립안 — 단 동일계열이라 완전독립 아님을 밝히고, 판돈 크면 Codex 복구 후 재교차.

## Agent Workflow (코드 작업 시)

1. **구현:** 루틴 구현은 `Gameplay` 위임, 게임감/코어/고레버 존은 오케스트레이터 직접(모델섹션대로). **단 캐릭터/몬스터 애니(공격 모션·상태 시퀀스·루트모션·전환)는 `Animation` 에이전트 전담** — ★헌법: ①한 동작 중엔 그 애니만(crossfade로 정체성 뭉개기 금지) ②공격=상태 시퀀스(접근→정지→공격) ③애니가 진실, 코드는 위치/포즈 안 만들고 상태전환·이벤트만.
2. **점검:** 로컬 검증(실행 가능하면 필수) 먼저. **Stab+Codex 병렬** 게이트 트리거 = §검증 게이트의 객관 클래스 — **재량 없음**. 저비용·가역 작업만 생략하되, 착공 전 면제 클래스를 한 줄로 명시(결과 + 미실행 검증 보고).

## Agent Aliases

`lv` LevelDesign(맵·배치·레벨) · `st` Story(서사·네이밍·캐넌 감사) · `sd` Sound(오디오 — 못 듣는 한계) · `gd` GameDesign(무기·성장·이코노미·런 구조; 제안→유저 판정, 동결권 없음) · `vc` VisualCritic(산출물↔레퍼 시각 대조 — 못 느끼는 한계; 미세 미적=`opus`) · `ch` Challenger(설계·방향·"됐다" 주장의 적대적 눈 — 반대 최강안+독립 대안. 코드=Stab·시각=vc가 못 덮는 *결정 고도* 담당. P0/P1 판단 비트만 발동). **각 역할 경계 상세 = 운영정책 doc.**

---

**작동 신호:** diff에 불필요한 변경 감소 · 과복잡 재작성 감소 · 실수 후가 아니라 구현 전에 질문.
